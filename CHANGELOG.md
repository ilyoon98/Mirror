# 버전 로그 — MIRROR RUSH

이 파일은 릴리스마다 갱신합니다. 최신 버전이 맨 위입니다.

---

## v1.14.0 — 2026-08-03

더 신나게, 그리고 박자에 맞춰 뛰는 UI.

BGM 이 얌전했습니다. 템포를 올리고 편성을 두껍게 한 뒤, 같은 박자를 화면으로도 흘려보냈습니다.

- **112 → 138 BPM.** 베이스가 4분음표에서 8분음표로 바뀌고, 킥·클랩·하이햇이 기본 편성에 들어왔습니다.
- **강도 단계를 3단 → 5단으로.** 홈(-1) / 기본(0) / 콤보 3+(1) / 콤보 6+(2, 리드 리프) / 피버(3, 16분 아르페지오).
- **홈 화면 BGM.** 인게임보다 잔잔한 편성입니다. 게임이 끝나면 정지하지 않고 홈 편성으로 넘어갑니다.
  브라우저 자동재생 정책 때문에 첫 입력(클릭·키) 이후에 시작합니다.
- **박자 동기 UI 펄스.** HUD 숫자·발사 버튼·시간 막대가 매 박 커졌다 작아지고, 마디 첫 박에는 더 크게 부풉니다.
  `setInterval` 이 아니라 **오디오 시계로 예약**하므로 소리와 화면이 어긋나지 않습니다.
- 음악을 꺼도 같은 BPM 의 대체 타이머로 펄스는 계속 뜁니다. **연출 끄기(F)** 를 하면 함께 멈춥니다.

---

## v1.13.0 — 2026-08-02

배경음악.

음원 파일을 받아 쓰면 저작권과 용량이 따라옵니다. 효과음과 똑같이 **Web Audio 로 합성**했습니다.

- Am - F - C - G 네 마디 반복, 112 BPM. 베이스(톱니) + 아르페지오(삼각) + 하이햇(노이즈) + 패드.
- **게임 상태에 반응합니다** — 콤보 3부터 아르페지오가 촘촘해지고, 피버에는 킥이 들어오며,
  남은 시간이 10초 아래로 떨어지면 낮은 긴장음이 깔립니다.
- 16분음표를 0.35초 앞서 예약하는 **look-ahead 스케줄러**입니다. `setInterval` 의 지터가 박자에 새지 않습니다.
- **B** 키 또는 하단 ♫ 버튼으로 토글. 설정은 기기에 남습니다. 소리를 끄면 음악도 함께 멈춥니다.

---

## v1.12.1 — 2026-08-01

이펙트가 엉뚱한 자리에서 터지던 문제 수정.

1×1 칸에서 일어난 일이 화면 반대쪽에서 터졌습니다. 클리핑으로 가려 놨던 증상의 **원인**을 고쳤습니다.

- SVG 요소의 `transform-origin` 기본값은 자기 위치가 아니라 **뷰박스 중심**입니다.
  `scale(5)` 를 걸면 원의 중심이 뷰박스 중심 쪽으로 최대 ±640 단위 끌려갔습니다.
- `fxRing` / `fxShrink` 에 `transform-origin` 을 셀 좌표로 명시했습니다. 수정 후 오차 0px.

---

## v1.12 — 2026-07-30

피버 성공 연출.

피버에서 맞혀도 점수 텍스트 하나만 뜨고 끝이라, 일반 맵 클리어보다 초라했습니다.
연쇄가 붙을수록 세지는 연출을 넣었습니다.

- **경로 타일이 뒤따라 켜집니다** — 빔이 지나간 순서대로 0.045초씩 밀려 점등.
- **출구에서 링이 터집니다** — 빔이 실제로 빠져나간 쪽(좌/우)에서 터지고,
  연쇄가 쌓일수록 링이 늘어납니다(2 → 6개).
- **불티가 사방으로 튑니다** — 연쇄에 따라 8 → 18개, 퍼지는 거리도 함께 커집니다.
- **최적 적중이면** 링이 초록으로 바뀌고 **레인 전체가 한 번 번쩍**입니다.
- **2연속부터 `N CHAIN`** 을 레인 아래에 크게 띄우고, 5연속부터 더 커집니다.
- 3연속부터 소리도 한 겹 덧입힙니다.

연출을 꺼 두면(`F`) 전부 건너뜁니다 — 게임 결과에는 영향이 없습니다.
이펙트 레이어는 `pointer-events:none` 이라 연출 중에도 탭이 그대로 먹습니다.

검증 — 적중 1~6회를 이어 쏘며 확인:
출구링 2→6, 불티 8→18 로 증가, 최적일 때만 초록 링·레인 번쩍,
연쇄 표시는 2회부터·확대는 5회부터, 각 이펙트는 수명 뒤 정리됩니다.


---

## v1.11 — 2026-07-30

순위표 이름 필터.

공개 순위표라 적은 이름이 그대로 노출되는데, 길이·제어문자·링크만 막고
표현 자체는 걸러내지 않고 있었습니다.

- 금지어 판정을 `core.js` 한 곳에 두어 **클라이언트와 서버가 같은 함수**를 씁니다.
  최종 판정은 서버이고, 클라이언트는 입력 즉시 알려 주는 용도입니다.
- 눈속임을 걷어내기 위해 비교 전에 정규화합니다 — 구분자·숫자 제거 후
  반복 축약을 두 가지(`loose`/`tight`)로 만들어 둘 다 검사합니다.
  `ㅅ.ㅂ`, `f u c k`, `fffuuuck` 모두 걸립니다.
- 사칭 우려가 있는 `admin`·`운영자` 류도 막습니다.
- 걸리면 오류로 막지 않고 **서버가 대신 이름을 지어 줍니다**(등재는 정상 진행).
- worker/index.html 의 자체 `cleanName`·`stripCtl` 중복 구현을 제거했습니다.

한계는 코드 주석에 적어 두었습니다 — 목록 방식이라 없는 표현은 통과하고,
정상적인 이름이 우연히 걸릴 수 있습니다. 그래서 거부가 아니라 대체로 처리합니다.

검증 — 이름 판정 27개 케이스 통과(정상 12 / 차단 15).
`뭉치`·`Anna`·`아아`·`book` 통과, `씨발`·`ㅅ.ㅂ`·`fffuuuck`·`admin`·링크 차단.


---

## v1.10 — 2026-07-30

시간 조작으로 점수를 무한히 올릴 수 있던 구멍을 막았습니다. (PROTOCOL mr6 → mr7)

### 무엇이 뚫려 있었나

v1.6 에서 판별 제한시간을 없애고 런 전체가 하나의 시간 풀을 쓰도록 바꾸면서,
**풀을 깎는 값이 클라이언트가 보고한 시간 하나**가 되었습니다.
모든 발사를 `e=0` 으로 보고하면 풀이 줄지 않아 런이 끝나지 않았습니다.

실측 — 발사 72번의 보고 시간 **합계 6ms** 로 레벨 71 · 랭크 563,418 이
검증을 그대로 통과했습니다. 당시 순위표 1위가 4,604점(레벨 15)이었으니 **122배**입니다.
더 밀면 랭크 503,412,707 까지 올라갔고, 서버가 요구하는 최소 경과 시간은 **0.0초**였습니다.

서버의 벽시계 대조(`minElapsedMs`)가 백스톱이어야 했지만, 그 값도
**보고된 시간을 더해서** 구하므로 0 을 보고하면 문턱도 0 이 되어 스스로 무너졌습니다.

### 네 겹으로 막았습니다

- **발사 최소 비용** (`MIN_SHOT_MS = 500`) — 보고값이 0 이어도 풀에서 500ms 를 깎습니다.
  클라이언트와 검증기가 같은 값으로 깎으므로 점수가 어긋나지 않습니다.
  사람이 새 판을 읽고 두 번 누르는 데 0.5초 미만이 걸리는 일은 드물어,
  정상 플레이(발당 약 1,500ms)에는 영향이 없습니다.
- **회복 총량 상한** (`REGAIN_CAP = 30000`) — PERFECT 회복(2,000ms)이 최소 비용(500ms)보다
  커서, 상한이 없으면 잘 쏘기만 하면 런이 무한히 이어졌습니다. 한 런에서 30초까지만 회복합니다.
- **피버 타격 상한** (`FEVER_MAX_TAPS = 25`) — 시각을 0 으로 채우면 한 구간에서
  무한히 두드릴 수 있었습니다.
- **콤보 배수 상한** (`COMBO_MULT_MAX = 4.0`) — 배수가 무한히 오르면 점수가 발사 수의
  제곱에 비례해, 발사 2.5배 차이가 점수 **35배** 차이가 됐습니다.
  상한을 두어 거의 선형으로 만들었습니다.
  덤으로 "오래 버티기" 보다 "잘 쏘기" 가 점수를 가르게 됩니다.

서버에도 **실제 경과 시간만으로 발사 수를 묶는 검사**를 추가했습니다
(`발사 수 ≤ 경과 / MIN_SHOT_MS + 5`). 보고값에 의존하지 않으므로 위 구멍과 함께
무너지지 않습니다. 런 유효 시간을 12시간 → 30분, 이벤트 상한을 5,000 → 2,500 으로 좁혔습니다.

### 남은 격차는 정직하게 적어 둡니다

| 시나리오 | 도달 레벨 | 랭크 | 정직 대비 |
|---|---|---|---|
| 정직 (1,500ms/발) | 55 | 123,977 | 1.0배 |
| 빠른 사람 (700ms/발) | 102 | 316,419 | 2.6배 |
| 조작 (0ms 보고) | 135 | 771,566 | **6.2배** |
| 조작 + 피버 40탭 | — | — | **거부됨** |

**무한이 6.2배로 내려왔지만 0 이 되지는 않았습니다.**
최소 비용(500ms)과 사람의 실제 속도(약 1,500ms) 사이의 차이가 그대로 남기 때문입니다.
더 줄이려면 최소 비용을 올려야 하는데, 그러면 정당하게 빠른 플레이어가 손해를 봅니다.
정당하게 빠른 사람이 이미 2.6배를 내므로, 남은 6.2배는 그 연장선 안에 있다고 보고 멈췄습니다.

### 검증

- 클라이언트↔검증기 점수 일치 3개 시나리오 통과 (700ms / 1,200ms / FLIP 섞기).
- 예전 익스플로잇 로그는 `피버 타격이 한 구간 상한을 넘음` 으로 거부됩니다.
- 레벨 생성기 불변식 유지 — 레벨 1~80 각 50판에서 풀 수 없는 판 0, 안전 경로 없는 판 0.
- 프리즘·단방향 관여율 유지 (100% / 88~98%).

---

## v1.9.1 — 2026-07-30

### 이펙트가 판 밖으로 날아가던 문제

보드 SVG 는 EXIT 레이블·화살표를 뷰박스 밖에 그리려고 `overflow:visible` 입니다.
그래서 v1.7~1.9 에서 커진 이펙트(링 6배, 파편 최대 56px)가 **버튼과 하단 설명 위까지**
퍼졌습니다. 특히 아래쪽 EXIT 에서 터질 때 눈에 띕니다.

- 이펙트 레이어(`#gFx`)에만 `clipPath` 를 적용했습니다 — 뷰박스 452 에 여유 28 을 더한
  `-28 … 480` 사각형. EXIT 표시는 `#gExit` 라 그대로 뷰박스 밖에 그려집니다.

### ON / OFF 를 색으로

라벨을 `연출 ON / OFF` 로 바꿨어도 글자만으로는 눈에 안 들어왔습니다.
`aria-pressed` 를 그대로 활용해 **켜짐은 시안색 테두리·글자·글로우, 꺼짐은 회색**으로
구분합니다. 아이콘과 단축키 표기까지 함께 흐려집니다.

### 버전 표기 x.x.x

배지·홈 하단·업데이트 내역을 모두 세 자리로 통일했습니다(과거 이력 17개 포함).

### 검증

- 이력 17개 전부 `x.x.x` 형식, 배지 `v1.9.1`, 홈 하단 표기 일치.
- 클립 사각형 좌표(`-28`, `508`)와 `#gFx` 적용 확인.
- 연출·소리 토글에서 `aria-pressed`·색·라벨이 **함께** 바뀌는 것 확인
  (`false/소리 OFF → true/소리 ON`).

**한계** — `clip-path` 는 `getBoundingClientRect()` 를 바꾸지 않아, 요소 좌표만으로는
실제로 잘렸는지 증명할 수 없습니다. 적용 여부는 계산된 스타일로 확인했고 육안 확인이
남았습니다.

---
## v1.9.0 — 2026-07-30

연출 패스. 버튼·HUD·클리어·타일 상호작용에 반응을 얹었습니다.
규칙은 건드리지 않았으므로 `PROTOCOL` 은 `mr6` 그대로이고 순위표도 유지됩니다.

### v1.9 후속 — 연출이 꺼져 있는 걸 알 수 있게 + 문구 배경판

**연출이 안 보인다는 제보의 원인은 설정이었습니다.** 초기값이 OS 의
`prefers-reduced-motion` 을 따르는데, 그 설정이 켜져 있으면 게임이 조용히 모든 연출을
끈 상태로 시작합니다. 그런데 하단 버튼이 `연출 끔` 이라고만 적혀 있어 이게 현재 상태인지
누르면 끄겠다는 뜻인지 알 수 없었습니다.

- 상태 라벨을 **`연출 ON / OFF`**, **`소리 ON / OFF`** 로 바꿨습니다.
- **꺼져 있으면 홈 화면에 금색으로 알려 줍니다** —
  `연출이 꺼져 있습니다 — F 키 또는 하단 ✦ 버튼으로 켜세요`.
  토글하면 즉시 갱신됩니다.

### 문구 배경판

화면 가운데 뜨는 `PERFECT!` / `MISS` 같은 문구가 거울·빔과 겹쳐 읽기 어려웠습니다.
문구를 감싸는 반투명 배경판(`.flash .plate`)을 깔았습니다 — 블러 + 시안 테두리.

- 내용에 맞춰 크기가 잡히고 `max-width: min(92%, 24rem)` 로 보드를 넘지 않습니다.
- **연출을 꺼도 배경판은 남깁니다.** 움직임이 아니라 가독성이라 애니메이션만 생략합니다.

검증 — 모바일 폭 375px 에서 짧은 문구 291×126, 긴 문구 276×126 으로 글씨를 감싸고
보드(351px) 를 넘지 않으며 가운데 정렬. 연출 OFF 에서도 배경판이 남는 것 확인.
라벨 초기값·토글 후 값, 홈 안내 표시/숨김도 확인했습니다.

### 버튼

- 누른 자리에서 **물결이 퍼집니다**(`.ripple`). `pointerdown` 을 문서에 한 번만 위임해
  `.btn / .ctl / .range / .tab / .verbtn` 전부에 적용됩니다.
- 눌림 반응(`scale(.955)`)과 **훑고 지나가는 광택**(hover).
- 조작 버튼도 hover 에서 시안 테두리·글로우가 붙습니다.

### HUD

- **점수·콤보가 오르면 금빛으로 튑니다.** 값이 바뀐 항목만 반짝이도록
  이전 값을 기억해 비교합니다(`hudPrev`).
- **남은 시간이 10초 아래면 붉게 맥박칩니다.** 타임바도 함께 밝기 펄스.
- 애니메이션을 다시 트리거하려면 클래스를 떼고 리플로우를 강제해야 하므로
  `pulse()` 헬퍼로 묶었습니다.

### 클리어 · 실패

- **클리어하면 EXIT 에서 터집니다** — 링 두 겹 + 파편 12개 + 잔불 5개.
  `×2` 출구는 금색, `×1` 은 초록입니다.
- **화면 전체 섬광**(`#fxFlash`) — PERFECT 금색 / 갈래 성공 보라 / 실패 붉은색 /
  방 확장·피버 시안. 요소 하나를 CSS 변수로 재사용합니다.
- 실패 시 남은 시간 값도 붉게 번쩍입니다.

### 타일 상호작용

- 레이저를 **설치하면 링**이 퍼집니다.
- **회전하면 그 방향으로 스파크**가 튑니다.
- **FLIP** 은 모든 거울 칸에서 순서대로 링이 퍼집니다(대각 거리순 지연).

### 접근성 · 성능

- 전부 `fxOn`(F 키)과 `body.nofx` 를 존중합니다. 초기값은 OS 의
  `prefers-reduced-motion` 을 따릅니다.
- 끄면 물결·섬광·HUD 펄스·광택·눌림 반응까지 모두 멈춥니다.
- 타일 이펙트는 v1.7 의 동시 상한(`FX_CAP` 140)을 그대로 씁니다.

### 검증 (브라우저 24항목)

- 헬퍼 존재, `#fxFlash` 요소, 버튼 물결 생성·자동 정리(0.7초),
  섬광 클래스·색 변수 반영.
- 점수·콤보 반짝, 시간 10초 경고 on/off.
- EXIT 폭발 19개 요소 생성 후 정리, 실제 발사에서 섬광 호출 및 동시 최대 19개.
- FLIP 섬광 + 거울 5개 링, 설치 링, 회전 스파크.
- `F` 끄기 상태에서 섬광·폭발·물결·HUD 펄스 모두 발생하지 않음.
- 콘솔 에러 0건.

---
## v1.8 — 2026-07-30

난이도를 **방 크기**로 밀던 것을 **거울 밀도 + 정답 희소성**으로 옮겼습니다.
판 생성이 달라지므로 `PROTOCOL` 을 `mr5 → mr6` 으로 올렸고 순위표가 새로 시작됩니다.

### v1.8 후속 — 지난 시즌 순위 보기

규칙이 바뀌면 순위표를 새로 시작하지만 **기록은 지우지 않습니다.** 그런데 자유 플레이는
옛 기록을 꺼내 볼 방법이 아예 없었습니다 — 서버가 조회 시 현재  로 고정했기
때문입니다. 순위표에 들어가면 이번 시즌 한 줄만 보이는 이유가 이것이었습니다.

-  — 지난 시즌 표를 볼 수 있습니다.
  값은  로 검사하고, 어긋나면 현재 시즌으로 떨어집니다.
- 응답에 (현재 시즌)와 (존재하는 시즌 + 런 수 + 최고점)를 함께 담아,
  클라이언트가 선택 UI 를 그릴 수 있게 했습니다.
- 순위 패널에 **시즌 칩**이 생겼습니다. 시즌이 둘 이상일 때만 나오고,
  현재 시즌은 금색으로 표시됩니다. 지난 시즌을 보면 안내문에  가 붙습니다.
- 오늘의 판 탭과 **결과 화면에서는 감춥니다** — 결과는 항상 현재 시즌입니다.

**규칙 변경이 아니라 읽기 기능이라  은 그대로  이고 순위표도 유지됩니다.**

검증 — 로컬 D1 에 mr2/mr4/mr5/mr6 기록을 심고 실제 워커로 확인했습니다.
시즌 칩 4개 렌더, 현재 시즌 표시·선택, 지난 시즌 mr4 조회 시 74,904점 기록 표시,
안내문 표시, 오늘의 판 탭에서 숨김, 자유 플레이 복귀 시 현재 시즌으로 복원,
지난 시즌을 보던 중 게임을 끝내도 결과는 현재 시즌으로 고정 (11+5 항목 통과).
잘못된  값은 현재 시즌으로 폴백하는 것도 확인했습니다.

### v1.8 후속 — 홈에서 순위표를 볼 때는 이름 입력을 감춤

홈 → 순위표는 **구경하는 자리**이고, 이름 입력은 **등재하는 자리**에서만 필요합니다.
결과 화면(`#screenBoard.resultmode`)에서만 이름칸·저장 버튼을 보여 줍니다.

- CSS 한 줄: `#screenBoard:not(.resultmode) .namebox{display:none}`
- 포커스도 모드로 갈랐습니다 — 결과 화면은 이름칸(비었을 때) 또는 `RETRY`,
  구경할 때는 `닫기`. 숨은 이름칸에 포커스를 주면 아무 데도 가지 않기 때문입니다.
- 이름은 게임이 끝난 뒤 결과 화면에서 적습니다. 한 번 저장하면 기기에 남아
  다음 판부터는 손댈 일이 없습니다.

### 왜 바꿨나

방이 커지면 두 가지가 나빠집니다. 60초를 런 전체가 공유하므로 **큰 판은 훑는 데만 시간을
더 먹고**, 7×7 에서는 타일 배율이 0.68 까지 줄어 **모바일 터치가 불편**했습니다.

그렇다고 밀도만으로는 안 됩니다. 5×5 를 고정하고 레벨만 올려 재보니
**레벨 13에서 거울 13개로 포화**하고, 빈칸이 8개로 줄어 **정답률이 9.6% → 23% 로 올라갑니다**
— 선택지가 줄어 오히려 찍어서 맞기 쉬워집니다. 밀도와 크기는 둘 다 물리적 상한이 있습니다.

상한이 없는 축은 **정답 희소성**(그 판을 클리어하는 발사가 몇 개인가) 하나뿐입니다.

### 변경

- **7×7 제거** — `boardSizeFor` 가 `lv<13 ? 5 : 6` 이 됐습니다.
- **거울 밀도 상한 0.55 → 0.62** — 6×6 에서 19개 → 22개.
  5×5 구간(레벨 1~12)은 거울이 최대 12개라 이 상한에 닿지 않아 영향이 없습니다.
- **정답 희소성** — 출구를 고를 때 허용하는 정답 수를
  `max(3, 6 - floor(level/10))` 으로 조입니다(레벨 1~9: 6, 10~19: 5, 20~29: 4, 30+: 3).

### 측정 (레벨당 60판)

| 레벨 | v1.7 방 | v1.7 정답률 | v1.8 방 | v1.8 정답률 | v1.8 최장 | v1.8 반사 |
|---|---|---|---|---|---|---|
| 13 | 6×6 | 14.4% | 6×6 | 14.1% | 12.5 | 6.9 |
| 20 | 6×6 | 18.4% | 6×6 | 16.5% | 14.7 | 9.8 |
| 30 | 7×7 | 15.1% | 6×6 | 20.5% | 15.8 | 11.3 |
| 50 | 7×7 | 17.1% | 6×6 | 20.8% | 13.5 | 9.1 |

희소성은 의도대로 작동합니다 — **거울 정답 수가 5.8개 → 3.6개로** 줄었습니다.

### 남은 것 — 후반 정답률의 절반은 프리즘

레벨 30에서 전체 23.2% 를 쪼개면 **거울 발사 11.4% + 프리즘 발사 11.9%** 입니다.
희소성은 거울 정답만 조이고, 프리즘 정답은 세지 않습니다(출구 후보를 고를 때
갈라지지 않는 경로만 봅니다).

희소성 하한을 3 → 2 로 더 내려도 23.2% → 23.2% 로 변화가 없었습니다.
더 낮추려면 프리즘의 보상 보장을 건드려야 하는데, 그러면 v1.5 에서 해결한
"프리즘은 위험만 있고 보상이 없는 함정" 문제가 되살아납니다. 여기서 멈췄습니다.

### 검증

- 판 3,000개(레벨 1~50 × 60) — 풀 수 없는 판 0, 프리즘 없이 클리어할 경로 없는 판 0,
  안전망(고정 배치) 도달 0.
- 판당 평균 6.21ms · 최대 31.3ms. 생성 시도는 평균 67.5회로 늘었습니다
  (희소성을 조인 대가, 상한 420회의 16%).
- 밀도 0.62 와 0.55 를 직접 비교해 0.62 를 택했습니다 — 0.55 는 경로가 짧아지고
  (14.9 → 12.9칸) 생성 시도가 늘었습니다(67.8 → 76.4회).

---
## v1.7 — 2026-07-30

피격 이펙트를 화려하게 키우고, 특수 타일 등장 시점을 앞으로 당겼습니다.
판 생성이 달라지므로 `PROTOCOL` 을 `mr4 → mr5` 로 올렸고 순위표가 새로 시작됩니다.

### v1.7 후속 — 결과 화면은 플레이한 모드로 고정

게임이 끝난 뒤 열리는 순위 화면에서 **자유 플레이 / 오늘의 판 탭을 감췄습니다.**
자유 플레이로 한 런을 오늘의 판 탭에서 보는 것은 의미가 없고, 방금 등재한 기록이
어느 표에 올라갔는지 헷갈립니다.

- 결과 화면()에서는 플레이한 모드의 순위만 보여 줍니다.
- 홈 → 순위표로 들어갈 때는 예전처럼 두 탭을 자유롭게 오갑니다.
- 자유 플레이 결과에서는  선택은 그대로 둡니다
  (같은 표 안에서 기간만 바꾸는 것이므로 혼동이 없습니다).

### 거울에 닿을 때 (가장 자주 보이는 장면)

예전에는 흰 링 하나뿐이라 눈에 잘 띄지 않았습니다. 이제 네 겹이 겹칩니다.

- 흰 **섬광** 코어
- **이중 링** — 흰색과 시안이 시간차로 퍼집니다
- **불꽃 10개** — 거울면(대각선)을 따라 양쪽으로 튑니다. `╱` 와 `╲` 가 다르게 튑니다
- **잔불 6개** — 주황빛 점이 위로 떠오르며 사라집니다

한 번 닿을 때 19개 요소가 뜹니다(예전 1개).

### 프리즘 · 소멸 타일

- **프리즘** — 보라·시안 이중 링에, **갈라지는 두 방향(진입 기준 좌·우 90°)** 으로
  불꽃을 뿜습니다. `tileFx` 에 진입 방향과 타일 값을 넘겨 방향성 있는 연출이 가능해졌습니다.
- **소멸 타일** — 링이 안으로 빨려들며 붉은 파편 12개가 터집니다.

### 특수 타일 등장이 빨라졌습니다

| | 예전 | v1.7 |
|---|---|---|
| 프리즘 | 레벨 8 | **레벨 6** |
| 단방향 거울 | 레벨 10 | **레벨 8** |

첫 피버가 5연속 클리어 직후(= 레벨 6 진입)이므로, 프리즘을 그 시점에 붙여
피버를 한 번 보고 나면 새 기믹이 열리는 리듬을 만들었습니다.

### 성능·접근성

- 동시 이펙트 상한 140개(`FX_CAP`) — 경로가 긴 발사에서 모바일이 무거워지지 않게.
- 연출 끄기(`F`)면 전부 건너뜁니다.
- 굵기를 방 크기 배율(`--k`)에 맞춰 6×6·7×7 에서도 비율이 유지됩니다.
- 이펙트의 `Math.random` 은 보기용이며 판 재현(`S.rng`)과 무관합니다.

### 검증

- 판 생성 1,500판(레벨 1~50 × 30) — 풀 수 없는 판 0, 프리즘 없이 클리어할 경로 없는 판 0,
  최대 갈래 4, 판당 평균 9.54ms · 최대 46.5ms.
- 등장 레벨 — Lv5 프리즘 0 / Lv6 프리즘 1, Lv7 단방향 0 / Lv8 단방향 1.
- 이펙트 — 거울 19개 · 프리즘 15개 · 소멸 14개 요소 생성, 자동 정리, 동시 상한,
  연출 끄기 존중.
- 실제 발사에서 `tileFx` 호출 확인(동시 최대 19개).

---
## v1.6 — 2026-07-30

**시간 규칙을 통째로 바꿨습니다.** 규칙 변경이라 `PROTOCOL` 을 `mr3 → mr4` 로 올렸고,
순위표가 새로 시작됩니다. 옛 기록은 지워지지 않고 `mr3` 로 남습니다.

### v1.6 후속 수정 2 — 패널을 화면 전체 모달로

- **패널이 보드 안에 갇혀 있던 것을 화면 전체 모달로 바꿨습니다.** 보드는 1:1 비율이라
  순위표·도움말처럼 내용이 긴 패널이 넘쳐 스크롤이 생겼습니다. 이제 화면 전체를 쓰고,
  내용이 짧으면 가운데, 길면 위에서부터 스크롤됩니다.
- **UI 깨짐 수정** — 보드에 떠 있던 고아 `<h2>튜토리얼</h2>` 태그를 제거했습니다.
  하단 조작 버튼 위에 글자가 겹쳐 보이던 원인입니다.
- **순위표가 안 나오던 버그 수정** — 코드 비교 기능을 없앨 때 `off.open` 참조가 남아
  `loadBoard()` 가 예외로 죽었습니다. 그래서 저장을 눌러도, 홈에서 순위표로 가도
  목록이 비어 있었습니다.
- **홈에서 순위표를 열면 `RETRY` 를 감춥니다.** 결과 화면일 때만 보입니다
  (`#screenBoard.resultmode`).
- **인게임에서 업데이트 내역·도움말을 열면 일시정지 화면이 겹쳐 뜨던 문제 수정.**
  패널을 여는 동안에는 화면 없이 시계만 멈추고, 닫으면 이어집니다.
  예전에는 일시정지 화면이 패널을 가리고, 닫을 때 시간이 흘러가 있었습니다.
- EXIT 레이블을 화살표에서 더 떼어 놓고 보드 위아래에 여유를 줬습니다.

### v1.6 후속 수정

- **게임이 끝나면 화면이 하나입니다.** GAME OVER 화면과 순위 화면이 따로 뜨던 것을 합쳐,
  최종 점수 · 이름 입력 · 순위표 · `RETRY` 를 한 화면에 담았습니다.
- **이름을 저장하면 그 이름으로 등재되고 순위표가 바로 갱신됩니다.**
  예전에는 이름을 적기 전에 등재돼 서버가 지은 이름으로 올라갔습니다.
  이름이 이미 저장돼 있으면 종료 즉시 등재하고, 없으면 저장을 기다립니다.
- **홈이 완전히 별도 화면이 됐습니다.** HUD(레벨·점수·콤보·시간) · 타임바 · 보드 ·
  조작 버튼 · 상단 바 · 하단 설명을 모두 감춘 깔끔한 화면입니다.
  홈에서 순위표·튜토리얼·도움말을 열면 그 패널만 뜨고, 닫으면 홈으로 돌아옵니다.

### 게임성 — 판별 제한시간 → 런 전체 60초

- 판마다 초기화되던 제한시간을 없애고 **런 전체가 하나의 시간 풀(60초)** 을 씁니다.
- **시간이 0이 되면 그 자리에서 게임 오버.** 라이프 개념을 폐지했습니다 —
  틀린 발사의 대가는 거기에 쓴 시간 그 자체입니다.
- 시간은 **플레이어가 판을 보고 있는 동안에만** 흐릅니다. 빔 연출 중에는 멈춥니다.
- **피버 구간에서는 시간이 줄지 않습니다.** 피버의 보상이 곧 '공짜 시간' 이 됐습니다
  (라이프가 없어졌으므로 기존 '라이프 +1' 보상은 제거).
- **PERFECT 한 발마다 2초 회복**, 상한 60초를 넘지 않습니다.
- **FLIP 횟수 제한 제거.** 두 번 쓰면 원래대로 돌아오고, 드는 비용은 흘러간 시간뿐입니다.
- 로그 의미 변경: `s` 이벤트의 `e` = '그 판을 보며 쓴 시간(ms)'.
  판별 시간초과를 뜻하던 `t` 이벤트는 없어졌습니다.

### UX

- **드래그가 타일을 옮기지 않습니다.** 누른 칸에 고정되고 방향만 바뀝니다.
  모바일에서 밀 때 설치 지점이 따라다니던 문제를 고쳤습니다.
- **설치 전에도 회전**할 수 있습니다. 미리 정한 방향으로 놓입니다.
- 이름 입력을 시작 화면에서 **순위 패널로** 옮기고 저장 버튼을 넣었습니다.
- 게임이 끝나면 **순위·이름 화면을 반드시 거치고**, 그 안에 `RETRY` / `홈으로` 가 있습니다.
- **'서버 없이 친구와 비교하기'(기록 코드) 기능을 제거했습니다.** 서버 순위표가 안정적으로
  동작하는 이상 대비책으로 둘 이유가 없어졌습니다. 자체 검증 표시는 남겼습니다.

### UI

- **홈 화면** 신설 — 시작(자유 플레이 / 오늘의 판) · 순위표 · 튜토리얼 · 도움말 · 내 최고 기록.
  시작 화면의 긴 설명문을 한 줄로 줄였습니다.
- **튜토리얼 5단계** 추가(읽는 형식). 단계 배열만 늘리면 되고, 나중에 고정 시드 연습판을
  붙일 자리를 비워 두었습니다.
- **이펙트** — 거울·단방향 피격 링, 소멸 타일 붉은 파열, 프리즘 갈라짐,
  PERFECT 시 `+2.0s` 플로팅. 연출 끄기(`F`)면 전부 생략됩니다.
- HUD `LIFE` → `TIME`, 도움말에 특수 타일·FLIP 설명 보강.

### 검증

- 검증기 직접 호출 — 정상 로그 통과, FLIP 판마다 4회 허용, 시간 소모가 풀에 정확히 반영,
  PERFECT 회복과 60초 상한 준수, 거부 3종(풀 초과 / 옛 `t` 이벤트 / 음수 시각).
- 브라우저 UI — 홈 문구 축약, 홈 메뉴, 튜토리얼 단계 이동, 설치 전 회전과 연속 회전,
  이름칸 위치, 순위 패널 RETRY, TIME HUD, 회복 상한, 코드비교 UI 제거 확인.
- 인라인 스크립트 문법 검사 통과, 콘솔 에러 0건.

**한계** — 피버를 넘나드는 로그의 클라이언트↔서버 점수 일치는 자동 검증하지 못했습니다.
브라우저 패널이 숨겨진 환경에서는 `requestAnimationFrame` 이 멈춰 게임 타이머를 흉내내야
하고, 중단된 테스트가 남긴 타이머가 rng 를 어긋나게 만들었습니다. 실제 플레이에서 결과
화면의 '자체 검증 통과' 표시로 확인하는 편이 확실합니다.

---
## v1.5 — 2026-07-30

**프리즘과 단방향 거울을 켰습니다.** 오래 막혀 있던 두 기믹이 처음으로 실제 플레이에
들어갑니다. 규칙이 바뀌었으므로 `PROTOCOL` 을 `mr2 → mr3` 로 올렸고,
**자유 플레이 순위표가 새로 시작됩니다.** 옛 기록은 지워지지 않고 `mr2` 로 남습니다.

### 프리즘 (레벨 8부터)

- 빔이 **좌 90°·우 90° 두 갈래**로 갈라집니다. 직진 갈래는 없습니다.
- 한 갈래라도 **EXIT** 로 나가면 클리어입니다. 죽은 갈래는 **그 갈래만 무효**이고
  판을 실패로 만들지 않습니다 — 한 갈래가 소멸 타일에 닿아도 괜찮습니다.
- 모든 갈래가 벽·소멸·맴돌기로 끝나면 실패입니다.
- **보드 밖으로 나간 갈래 수**가 배율입니다 — 2갈래 1.25배 · 3갈래 1.6배 · 4갈래 2배.
  정답 출구가 아니어도 보드를 벗어나기만 하면 셉니다.
- 한 갈래가 프리즘을 최대 2번 지날 수 있어 최대 4갈래입니다.

### 단방향 거울 (레벨 10부터)

- **위나 왼쪽에서 들어오면 통과**, **아래나 오른쪽에서 들어오면 반사**합니다.
- 지나는 방향에 따라 지름길이 되기도, 막는 벽이 되기도 합니다.
- 평면 단방향 거울(`-`, `|`)은 추가하지 않기로 확정했습니다.

### 검증

이번 릴리스는 **실제 게임 루프를 구동해** 확인했습니다.

- **클라이언트 점수 = 서버 검증 점수, 9개 런 전부 일치.** 프리즘 발사 2~7회,
  FLIP 1~2회, 피버 1~2라운드, 라이프 손실, 흡수된 갈래가 있어도 성공한 사례
  0~7건이 포함된 런들입니다. 최대 갈래 수 실측 4.
- 판 생성 2,000판에서 풀 수 없는 판 0, 프리즘 없이 클리어할 경로 없는 판 0, 안전망 0.
- 콘솔 에러 0건.

검증 중 불일치가 한 번 났는데 **테스트 하니스의 결함**이었습니다.
피버 연출이 끝나기 전에 강제로 피버를 종료시켜 실제 플레이에서 나올 수 없는 순서를
만들었습니다(대기 조건을 고치자 사라졌습니다). 이 경계는 v1.1 에서
'피버 종료 직전 발사' 로 이미 고쳐 둔 지점입니다.

### 화면

- 도움말에 **소멸 타일 · 프리즘 · 단방향 거울 · FLIP** 설명을 추가했습니다.
- 프리즘 스크린리더 안내를 '세 갈래' → '좌우 두 갈래' 로 고쳤습니다.

---

## 미출시 → v1.5 에 포함 — SPLIT_BONUS 확정 + 단방향 관여율 복구

(v1.5 로 출시됐습니다. 아래는 그때의 작업 기록입니다.)

### 확정 — `SPLIT_BONUS = [1, 1, 1.25, 1.6, 2.0]`

out=2 가 성공의 약 62% 를 차지해 실질 조절 지점은 인덱스 2 하나입니다.
프리즘 발사는 배율 이전에 이미 일반 발사의 1.5~2.7배(기본점수 136~239 대 기준선 88)
이므로 배율을 세게 주면 프리즘이 사실상 필수가 됩니다.

확정 수치의 실측 결과: **성공 시 평균 209점(일반 발사의 2.4배), 기대값 98점(1.1배)**.
지시문 작성 시 참고한 값(2.6배 / 1.2배)보다 조금 낮게 나왔습니다 — 배치 순서 수정으로
기본점수 분포가 바뀌었기 때문입니다(out=2 평균 179, 기준선 88).
2.6배 / 1.2배를 그대로 맞추려면 인덱스 2 를 `1.35` 로 올리면 됩니다.

### 수정 — 단방향 관여율 복구 (배치 순서)

프리즘을 **단방향보다 먼저** 놓도록 순서를 바꿨습니다.
단방향을 먼저 놓으면 뒤에 놓인 프리즘이 단방향이 쓰던 정답 경로를 끊어버립니다
(프리즘은 들어온 갈래를 소비합니다). 프리즘을 먼저 놓고 그 위에서 단방향을 검증하면
이 문제가 생기지 않습니다.

`onSolutionPath` 도 엄격하게 고쳤습니다. `simulate`(= `beams[0]`)를 쓰던 것을
`trace` + `splits===0` 로 바꿔, **갈라지지 않는 정답 경로만** 관여로 인정합니다.
`solveInfo` 의 `ok` 판정과 기준을 맞춘 것입니다.

같은 기준으로 A/B 측정한 결과 (레벨 1~50 × 40판, 단방향 등장 판 1,640):

| | 단방향 → 프리즘 (수정 전) | 프리즘 → 단방향 (수정 후) |
|---|---|---|
| 관여율 (엄격: 갈라지지 않는 경로) | 76.1% | **94.1%** |
| 관여율 (기존 측정과 같은 느슨한 기준) | 84.1% | **95.5%** |
| 지름길 **판 수** | 403 | **392** |
| 지름길 비중 | 32.3% | 25.4% |

지름길 비중이 내려간 것은 **분모가 커졌기 때문**입니다. 지름길 판 수는 403 → 392 로
사실상 그대로이고, 새로 관여하게 된 약 295판이 주로 '동일'·'길어짐' 이었습니다.
기존에 보고한 37.4% 는 3갈래 시절 + 느슨한 기준으로 잰 값이라 직접 비교되지 않습니다.

남은 5.9%(97판)의 원인을 분해했습니다.

- 애초에 관여 가능한 자리가 없는 판 **67.0%** (65판) — 구조적으로 불가
- 프리즘·두 번째 단방향과의 상호작용 26.8% (26판)
- 소멸 타일이 끊음 6.2% (6판)

3분의 2가 구조적 한계라, 배치 후 재확인·재배치 패스를 하나 더 붙여도 상한은 약 96%
입니다. 비용 대비 이득이 작아 넣지 않았습니다.

### 재측정 (레벨 1~50 × 40판 = 2,000판)

- 프리즘 성공률 **46.8%** (닿은 발사 16,717건). 배치 순서 수정으로 45.0% → 46.8%
- '공짜 성공' 12.4% (직진이 닿던 출구와 동일한 성공 5.1%) — 규칙이 만드는 공짜는 없음
- 갈래 결말 — 출구 23.0% / 벽 52.5% / 흡수 19.7% / 무한반사 3.2% / 예산소진 1.6%
- 갈래 수 — 2갈래 79.4% / 3갈래 19.3% / 4갈래 1.3%
- out 별 성공률·기본점수 — 1: 41.5%/136, 2: 50.5%/179, 3: 63.6%/220, 4: 54.4%/239
- 생성 비용 — 시도 횟수 55.25회/판(변화 없음), 판당 7.76 → 9.04 ms(1.16배), 최대 56.1 ms.
  `onSolutionPath` 를 `trace` 기반으로 바꾼 비용입니다. 체감 지연 없음
- 안전성 — 2,000판에서 풀 수 없는 판 0, 프리즘 없이 클리어할 경로 없는 판 0,
  안전망 도달 0, 최대 갈래 수 4

---

## 미출시 → v1.5 에 포함 — 프리즘 규칙 확정: 직진 제외, 좌우 2갈래

(v1.5 로 출시됐습니다. 아래는 그때의 작업 기록입니다.)

### 변경 — 프리즘은 좌 90°·우 90° 두 갈래만

직진 갈래를 아예 만들지 않습니다. 기존 "진입 반대편만 제외한 3갈래(직진+좌+우)" 를
대체합니다.

이전 측정에서 프리즘 성공의 43.1% 가 **'공짜 성공'** 이었습니다 — 프리즘이 정답 경로
위에 놓이니 직진 갈래가 원래 경로를 그대로 이어받아, 프리즘이 없어도 성공했을 발사에
평균 1.98배 배율만 얹혔습니다. "직진 갈래를 보너스에서 제외" 같은 예외 처리 대신
**직진 자체를 없애서** 그 경우가 구조적으로 생길 수 없게 했습니다.

- `MAX_SPLITS` 의 의미를 **전역 예산 → 갈래별 깊이**로 바꿨습니다.
  지시문의 "최대 4갈래(1회당 2갈래 × 2회 분기 = 2²)" 는 깊이 기준일 때만 성립합니다.
  전역 예산이면 최대 3갈래가 됩니다. 실측 최대 갈래 수 4로 확인했습니다.
- 분기 예산을 다 쓴 갈래는 프리즘에서 **멈춥니다**(직진 통과시키지 않음).
  전체 갈래의 1.9% 이고, 다른 죽은 갈래와 같이 그 갈래만 무효입니다.
- 판정 규칙은 그대로입니다 — 흡수·벽·무한반사는 그 갈래만 무효, 최소 1갈래가 실제
  출구에 닿으면 클리어, 전부 죽으면 실패. 보너스는 보드를 벗어난 갈래 수로 계산.
- `placeHazards` 의 배치 후 재검증 로직은 손대지 않았습니다.

### 측정 (레벨 1~50 × 40판 = 2,000판)

**생성 비용 — 회귀 없음**

| | 직진 포함 3갈래 | 직진 제외 2갈래 |
|---|---|---|
| genLevel 시도 횟수 | 55.25회/판 | 55.25회/판 (1.00배) |
| 판당 생성 시간 | 7.62 ms | 7.80 ms (1.02배) |
| 최대 생성 시간 | 65.9 ms | 60.8 ms |

**프리즘 성공률 — 65.5% → 45.0%** (프리즘에 닿은 발사 16,953건)

**'공짜 성공' — 43.1% → 13.4%.** 0% 은 아니지만 성격이 달라졌습니다.
목표였던 '직진이 경로를 이어받는' 경로는 사라졌고(같은 출구로 성공하는 경우 5.9%,
이건 꺾인 갈래가 우연히 같은 면에 닿은 것), 남은 13.4% 는 **그 발사가 프리즘이 있든
없든 풀리는 판이었다**는 뜻입니다. 점수도 프리즘 쪽이 높은 경우가 45.7% 뿐이고
27.5% 는 오히려 낮습니다(평균 1.48배). 규칙이 만드는 공짜 배율은 없어졌고,
남은 것은 퍼즐 기하학상의 우연이라 0% 으로 만들 수는 없습니다.

**갈래 결말** — 출구 21.9% / 벽 54.2% / 흡수 18.2% / 무한반사 3.7% / 예산소진 1.9%

**갈래 수** — 2갈래 78.7% · 3갈래 20.1% · 4갈래 1.2%

**보드를 벗어난 갈래 수(out)별**

| out | 전체 발사 | 성공 | 성공률 | 성공 시 기본점수 평균 |
|---|---|---|---|---|
| 1 | 5,345 | 2,042 | 38.2% | 151 |
| 2 | 10,055 | 4,882 | 48.6% | 178 |
| 3 | 1,021 | 657 | 64.3% | 235 |
| 4 | 45 | 42 | 93.3% | 351 |

기준선: 프리즘을 쓰지 않는 일반 성공 발사 12,601건의 기본점수 평균 **84**.
즉 프리즘 발사는 **배율 없이도 이미 1.8~4.2배**입니다. 꺾인 갈래가 길게 돌기 때문입니다.
`SPLIT_BONUS` 를 설계할 때 이 점이 핵심입니다.

**단방향 관여율 — 97.9% → 77.6%** (지름길 19.8% / 동일 51.1% / 길어짐 29.1%).
낮아진 이유는 배치 순서입니다. 단방향을 먼저 놓고 프리즘을 나중에 놓는데, 프리즘이
빔을 소비하게 되면서 단방향이 관여하던 경로가 프리즘 배치로 끊기는 경우가 생깁니다.
순서를 바꾸거나 프리즘 배치 후 단방향 관여를 다시 확인하면 회복될 것으로 보입니다.

**안전성** — 2,000판에서 풀 수 없는 판 0, 프리즘 없이 클리어할 경로가 없는 판 0,
안전망 도달 0.

### 단방향 거울 — 최종 확정 (변경 없음)

평면 단방향 거울(`-`, `|`)은 추가하지 않기로 확정했습니다.
지금의 대각선 단방향(`╱`/`╲`, E·S 진행은 통과, W·N 진행은 반사) 스펙 그대로입니다.

---

## v1.4 — 2026-07-30

플레이어가 이름을 직접 적게 하고, v1.3 이 만든 등재 실패 버그를 고쳤습니다.
프리즘·단방향 거울의 배치 구조도 함께 고쳤지만 **아직 켜지 않았습니다**(배율 미정).

### 수정 — 다른 사람이 등재에 실패하던 버그

v1.3 의 시드 쇼핑 방어를 **IP 기준**으로 만든 것이 원인입니다.
같은 네트워크(집·사무실·모바일 NAT)의 두 사람이 한 런을 공유해 버렸습니다.

```
A: /api/run → 런#1 생성
B: /api/run → 런#1 을 그대로 받음   ← 같은 IP
A: 제출 → 런#1 done
B: 제출 → 409 "이미 제출된 런입니다"
```

같은 판이 나오는 문제도 같은 원인이었습니다. 실제 DB 를 확인해 보니 등재된 런이
전부 하나의 `ip_hash` 에서 나와 있었습니다.

- 재사용 기준을 **기기 식별자(`client_id`)** 로 바꿨습니다. 브라우저가 임의 문자열을
  만들어 `localStorage` 에 보관하고, 서버는 '같은 기기인가'만 봅니다.
- 새로고침으로 날린 런을 이어받는 동작은 그대로입니다.
- `client_id` 를 안 보내는 옛 클라이언트는 예전처럼 IP 로 묶습니다(서로 섞이지 않게 분리).

**정직하게 적어 둘 한계:** `client_id` 는 클라이언트가 만드는 값이라 매번 새로 만들면
재사용을 피할 수 있습니다. 즉 재추첨을 실제로 막는 것은 IP 기준 시간당 상한이고,
재사용은 '새로고침 복구 + 손쉬운 재추첨 차단' 수준입니다. 정상 플레이가 깨지는 것보다는
이쪽이 옳은 절충이라고 판단했습니다.

- 시간당 상한을 두 층으로 나눴습니다 — 기기 기준 40회, IP 기준 200회.
  IP 상한만 40 이면 사람 여럿이 쓰는 공유 IP 가 막힙니다.

### 추가 — 순위표에 쓸 이름을 직접 입력

- 시작 화면에서 이름을 적습니다. **이 기기에 저장돼 다음 판에도 같은 이름으로 등재**됩니다.
  매 판 이름이 달라지던 문제가 없어집니다.
- 비워 두면 예전처럼 서버가 지어 줍니다(`또렷한각도-300` 형태).
- 정리 규칙은 **서버가 최종 판정**합니다 — 12자(코드포인트 기준, 이모지 안 쪼갬),
  연속 공백 한 칸으로, 제어·폭 없는 문자 제거, 링크 거부.
- 이름은 순위표와 기록 코드에 함께 쓰입니다.

**설계 방향이 바뀐 지점입니다.** v1.1 에서는 "부적절한 표현·개인정보 관리 책임을
지지 않기 위해 자유 입력을 받지 않는다"고 정했는데, 이번에 그 결정을 뒤집었습니다.
표현 자체는 걸러내지 않으므로, 문제가 생기면 `worker/index.js` 의 `cleanName` 에
금지어 목록을 추가해야 합니다.

### 내부 — 프리즘·단방향 배치 구조 수정 (비활성)

`placeHazards` 를 **배치 후 회피 → 배치 후 재검증**으로 바꿨습니다.
소멸 타일은 예전 방식 그대로입니다.

- 프리즘·단방향을 보호구역 안(정답 경로 위)까지 후보에 넣고, 놓은 뒤 판이 여전히
  풀리는지 재시뮬레이션해 통과할 때만 확정합니다.
- 판정 규칙 변경: 프리즘 갈래의 흡수·벽·무한반사는 **그 갈래만 무효**이고 판을
  실패로 만들지 않습니다. 최소 1갈래가 실제 출구에 닿으면 클리어입니다.
  일반 발사의 '흡수 = 실패' 는 그대로입니다.
- 보너스 배율은 **보드를 벗어난 갈래 수**로 셉니다(정답 출구가 아니어도 포함).
- 소멸 타일 배치 전에 보호구역을 다시 계산합니다. 프리즘이 예전 최적 경로 위에
  놓이면 넘겨받은 보호구역이 더는 살아 있는 경로가 아니어서, 소멸 타일이 남은
  유일한 정답 경로를 막을 수 있었습니다.

측정 결과 (레벨 1~50 × 40판 = 2,000판):

| | 회피(기존) | 재검증(신규) |
|---|---|---|
| 프리즘 — 1갈래 이상 출구 도달 | 0.0% | 65.5% |
| 단방향 — 정답 경로 관여 | 0.0% | 97.9% |
| genLevel 시도 횟수 | 56.30회/판 | 56.30회/판 |
| 판당 생성 시간 | 15.98 ms | 7.81 ms |

생성 비용은 늘지 않았습니다. 기존 코드가 찾을 수 없는 자리를 매번 전수 스캔하고
있었기 때문에 오히려 절반으로 줄었습니다.

프리즘 성공 5,700건 중 **43.1% 는 프리즘이 없어도 성공했을 발사**였습니다
(직진 갈래가 원래 정답 경로를 이어받아 평균 1.98배 배율만 얹힘).
남은 56.9% 는 꺾인 갈래가 만든 진짜 새 해법입니다.
이 '공짜 배율' 처리와 `SPLIT_BONUS` 수치를 정한 뒤 켤 예정입니다.
켤 때는 규칙이 바뀌므로 `PROTOCOL` 을 `mr3` 로 올려야 합니다.

### 데이터베이스

`runs` 에 `client_id` 를 더합니다.
[`worker/migrations/002-client-id.sql`](worker/migrations/002-client-id.sql) 로 반영합니다.

### 검증

- **플래그 OFF 회귀: HEAD 와 완전히 동일** — 판 1,000개 바이트 일치,
  `judgeShot` 7,472발 결과 일치, 리플레이 점수 일치, `PROTOCOL` `mr2` 유지.
- 플래그 ON 안전성: 2,000판에서 풀 수 없는 판 0, 안전한 정답 경로 없는 판 0,
  안전망 도달 0. 정답 경로 위에 타일을 놓아도 프리즘 없이 클리어할 길이 남습니다.
- 실제 `wrangler dev` + D1 종단 14/14 통과 — **같은 IP 다른 기기 두 사람이 각자
  다른 판을 받고 둘 다 등재**(예전엔 뒤쪽이 409), 같은 기기 재요청은 재사용,
  제출 후 새 판, 두 판 연속 같은 이름, 이름 정리 4종, 순위표 표시.
- 클라이언트 13/13 통과 — 기기 식별자 고정, 이름 저장·복원, 12자 절단, 공백 정리,
  링크 거부, 제어문자 제거, 이모지 유지.

---

## v1.3 — 2026-07-30

**자유 플레이가 메인이 되었습니다.** 순위표의 주력도 자유 플레이로 옮겼습니다.

### 왜 비교가 성립하는가

v1.1 에서 시드를 도입한 이유는 "매 판이 랜덤이면 점수 차이가 실력 차이인지
판 난이도 차이인지 알 수 없다"였습니다. 자유 플레이를 순위표에 올리는 것은
그 논리와 정면으로 부딪히는 것처럼 보입니다.

부딪히지 않습니다. 문제가 되는 것은 **한 판**을 비교할 때입니다.
자유 플레이의 한 런은 레벨 1부터 같은 생성기·같은 난이도 곡선을 순서대로 밟으며
수십 판을 풉니다. 판 하나하나의 운은 런 전체에서 평균으로 씻겨 나가므로,
런 총점은 비교 대상이 됩니다. 엔드리스 아케이드가 조각 순서가 달라도
하나의 순위표를 쓰는 것과 같은 이유입니다.

반대로 **오늘의 판**은 단 하나의 판을 푸는 형식이라 시드 고정이 필수입니다.
그래서 두 순위표를 성격이 다른 채로 나란히 둡니다.

### 추가 — 자유 플레이 순위표

- `/api/board?mode=free` — 같은 규칙 버전(`protocol`)의 모든 런을 한 표에 모읍니다.
- `?days=7` 로 기간을 좁힐 수 있습니다. 전체 기간만 두면 상위권이 굳어
  신규 진입이 막히기 때문입니다. 화면에 `전체 / 최근 7일` 선택을 두었습니다.
- 순위 패널 기본 탭이 자유 플레이입니다. 오늘의 판은 보조 탭으로 남습니다.
- 등재 순위를 세는 범위도 모드마다 다릅니다 —
  자유 플레이는 같은 규칙 버전 전체, 오늘의 판은 같은 시드끼리.

### 변경 — 시드를 서버가 발급합니다

자유 플레이는 그동안 클라이언트가 시드를 만들었고, 그래서 등재 대상이 아니었습니다.
순위에 올리려면 **스스로 고른 시드를 인정할 수 없습니다.**

- 판을 그리기 전에 `/api/run` 응답을 기다려 **서버가 준 시드를 그대로** 씁니다.
- `freeSeed()` 를 코어에 두어 서버와 오프라인 플레이가 같은 형식을 씁니다.
  오늘의 판처럼 규칙 버전을 앞에 붙이므로(`mr2-f…`) 규칙이 바뀌면 순위표도 새로 시작합니다.
- 서버에 닿지 못하면 게임은 그대로 진행되고 **등재만 빠집니다.**
  순위 패널에 이유를 표시합니다.

### 추가 — 시드 쇼핑 방어

자유 플레이가 순위표의 주력이 되면서 새로 생긴 구멍입니다. 오늘의 판은 시드가
하나라 고를 수 없었지만, 자유 플레이는 `/api/run` 을 반복해 유리한 시드만
골라 쓸 수 있습니다. 판 생성 규칙이 공개된 결정론 코어이므로 받은 시드의
유리함을 클라이언트가 즉시 계산할 수 있습니다.

- **열려 있는 런이 있으면 같은 런을 그대로 돌려줍니다**(10분). 새 시드를 받으려면
  그 런을 끝내 제출하거나 10분을 기다려야 하므로, 시드 한 장의 비용이
  "무료 무한 재추첨"에서 "런 하나 또는 10분"으로 올라갑니다.
- 부작용: 게임 중간에 이탈하고 다시 시작하면 같은 판이 나옵니다.
  정상적으로 게임 오버까지 가면 자동 제출되어 새 판을 받습니다.
  새로고침으로 날린 런을 이어받는 효과도 있습니다.
- 같은 IP 의 시간당 런 시작을 40회로 제한했습니다(자동화 backstop).
- **IP 는 평문으로 저장하지 않습니다.** 순위표에 필요한 정보가 아니고 있으면
  유출 대상이 되기만 하므로, 솔트를 섞은 해시 32자만 남깁니다.

### 수정 — v1.2 에서 깨져 있던 날짜 표시

`dailySeed` 에 규칙 버전 접두어가 붙으면서(`mr2-20260730`) 화면이 시드를
`slice(0,4)` 로 잘라 쓰던 두 곳이 깨져 있었습니다.
시작 화면은 `07/30` 대신 **`20/26`**, 순위 패널은 **`mr2-.2026.0730`** 을 보여주고 있었습니다.
접두어를 건너뛰고 잘라내도록 고쳤습니다.

### 데이터베이스

`runs` 에 열을 둘 더합니다. 이미 배포된 D1 은
[`worker/migrations/001-free-board.sql`](worker/migrations/001-free-board.sql) 로 반영합니다.

- `protocol` — 자유 플레이 순위를 묶는 기준
- `ip_hash` — 열린 런 재사용·시간당 상한 조회용(해시)

### 검증

- 워커 15개 시나리오 통과 — 시드 발급, 열린 런 재사용, 다른 IP 분리,
  경과시간 부족 거부, 정상 제출, 재제출 거부(409), 제출 후 새 시드,
  자유 순위표 집계, 기간 필터, 오늘의 판 분리, `seed=` 옛 호출 호환,
  불가능한 수 거부, 시간당 상한(429), 평문 IP 미저장, health.
- 클라이언트 종단 확인 — 서버 시드 채택, 로그 제출, 결과 화면
  `자유 플레이 전체 3위` 표시, 순위표 렌더·내 기록 강조·기간 전환.
- 콘솔 에러 0건. 오프라인(`file://`)에서 자유 플레이 정상 동작.
- **한계:** 이 PC 의 Node 가 20.9 라 wrangler(22+ 필요)를 실행할 수 없어
  D1 은 메모리 대체 구현으로 검증했습니다. **SQLite 문법 자체는 실행 검증되지
  않았으므로**, 배포 전에 `wrangler dev` 로 한 번 확인하세요.

---

## v1.2 — 2026-07-30

요청한 4가지 기믹 중 2개를 완성하고, 나머지 2개는 구조적 문제를 확인해 잠가 뒀습니다.
규칙이 바뀌었으므로 `PROTOCOL` 을 `mr1 → mr2` 로 올려 옛 기록과 새 판이
순위표에서 섞이지 않게 했습니다.

### 추가 — 소멸 타일

- 빔이 소멸 타일에 닿으면 흡수되어 실패합니다(실패 사유 `장애물에 흡수되었습니다`).
- 레벨당 1~3개, 방 크기에 비례한 상한. **두 출구의 최적 경로는 보호**하므로
  항상 클리어할 수 있습니다.
- 실측: 레벨 1~50 각 60판에서 풀 수 없는 판 0건, 안전 경로 없는 판 0건.

### 추가 — FLIP 버튼

- 판마다 1회, 모든 거울이 `╱ ↔ ╲` 로 동시에 뒤집힙니다(고정 규칙이라 암산 가능).
- 발사 전에만 쓸 수 있고, 사용 후 `aria-disabled` + 스크린리더 안내.
- 리플레이 로그에 `x` 이벤트로 남아 서버 검증을 통과합니다.
- `applyFlip()` 을 코어에 두어 클라이언트와 검증기가 같은 함수를 쓰게 했습니다.
  (초기 구현에서 FLIP 후 `maxScore` 재계산이 한쪽에만 있어 점수가 어긋났습니다)

### 보류 — 프리즘 (`PRISM_ENABLED = false`)

3방향 분산·흡수 판정·분산 보너스가 모두 구현되어 있으나 껐습니다.
출구는 "정답이 6개 이하인 희귀한 면"으로 고르는데 갈래는 임의 방향으로 흩어져
그 면에 닿을 확률이 사실상 0입니다. 전수 배치 탐색으로도 성공 가능한 판이 0%였습니다.
현행 규칙 그대로면 위험만 있고 보상이 없는 함정이라 켜지 않았습니다.

### 보류 — 단방향 거울 (`ONEWAY_ENABLED = false`)

통과/반사 규칙(E·S 진행은 통과, W·N 진행은 반사)과 렌더링은 완료했습니다.
특수 타일을 정답 경로를 피해 배치하는 구조라, 정답에 관여하는 판이 0%였습니다.
경로 위에 놓고 재검증하는 별도 배치가 필요합니다.

### 검증

- 타일 기믹 단위 테스트: 흡수 / 3방향 분산 / 통과·반사 / FLIP 모두 규칙대로 동작.
- 클라이언트↔검증기 점수 일치 4개 시나리오 통과
  (최적만 / FLIP 3회 / 흡수 2회 / 전부 혼합).
- 레벨 생성 평균 0.3~13ms, 안전망 도달 0건.

---

## v1.1 — 2026-07-30

기록 비교 기능의 1단계. 게임을 **시드 기반 결정론**으로 바꾸고 오늘의 판과 기록 코드를 추가했습니다.

### 왜 시드부터인가

기록 비교에는 벽이 세 개 있었습니다.

1. 정적 파일이라 남의 기록을 담을 곳이 없다
2. 클라이언트 점수는 콘솔 한 줄이면 조작된다
3. **매 판이 랜덤이라 애초에 비교가 성립하지 않는다** — 점수 차이가 실력 차이인지
   판 난이도 차이인지 알 수 없습니다

3번이 가장 근본적이고, 시드화가 세 문제를 동시에 푸는 열쇠입니다.
같은 시드면 판이 완전히 같아지고(2·3), 입력 로그만 있으면 점수를 재현할 수 있어
서버 검증이 가능해집니다(2).

### 추가 — core.js (결정론 코어)

판 생성·시뮬레이션·점수 계산을 `core.js` 한 파일로 분리했습니다.
**브라우저와 검증 서버가 같은 파일을 그대로 실행**하므로 규칙이 어긋날 수 없습니다.

- `mulberry32` 시드 PRNG. `Math.random()`이 게임 진행에 쓰이던 7곳을 전부 대체했습니다.
- `replay(seed, events)` — 입력 로그를 되돌려 점수를 재현하고, 불가능한 수는 거부합니다.
- Node에서 실행해도 브라우저와 **완전히 같은 판**이 나오는 것을 확인했습니다
  (Lv1 출구 `0,4,E ×1 / 0,3,N ×2`, maxScore 80 일치).

### 추가 — 오늘의 판

- 시드가 **UTC 날짜**입니다. 지역마다 다른 판을 받으면 비교가 깨지므로 UTC로 통일했습니다.
- 시작 화면에서 `오늘의 판` / `자유 플레이`를 고릅니다. 자유 플레이도 시드를 쓰므로
  모든 판이 재현·검증 가능합니다.
- 오늘의 판 최고 기록을 `localStorage`에 보관해 시작 화면에 표시합니다.

### 추가 — 기록 코드로 비교

- 결과 화면의 `기록 비교`에서 `mr1|20260729|4820|17|거울696|C86` 형태의 코드를 복사합니다.
- 친구 코드를 붙여넣으면 같은 목록에 순위가 매겨집니다.
  **시드가 다르면 거부합니다** — 다른 판끼리 비교하는 건 의미가 없기 때문입니다.
- 체크섬은 오타·장난만 걸러냅니다. 위조를 막지는 못하며, 이건 2단계(서버 검증)의 몫입니다.

### 변경 — 랭킹 점수 분리

화면 점수와 별도로 **시간 보너스를 뺀 `랭킹 점수`**를 계산합니다.
시간 보너스는 클라이언트가 보고한 시각에 의존해 서버가 검증할 수 없으므로,
비교 기준에서 빼는 편이 정직합니다. 시간 압박은 "제한시간 안에 못 풀면 라이프를 잃는다"는
형태로 여전히 작동합니다.

### 수정 — 검증이 어긋나던 두 지점

실제 플레이 로그를 검증기에 넣어 보고 찾은 불일치입니다.

- **피버 종료 직전 발사** — 빔이 나는 동안 피버가 끝나면 클라이언트는 그 발사를 무시하는데
  로그에는 남아, 검증기의 rng 소비가 한 번 어긋나 이후 판이 전부 달라졌습니다.
  로그를 발사 시점이 아니라 **실제로 처리되는 시점**에 남기도록 고쳤습니다.
- **실패·시간초과 시 레벨** — 클라이언트는 레벨을 올리지 않고 같은 레벨의 새 판을 내는데
  검증기는 레벨을 올리고 있었습니다.

### 검증

- 콘솔 에러 0건, `core.js`·`index.html` 인라인 스크립트 파싱 정상.
- 결정론: 같은 시드 30판 완전 동일, 다른 시드는 상이. 피버 레인도 동일.
- **클라이언트 ↔ 검증기 점수 일치 5개 시나리오 전부 통과**
  (전부 최적 / 실패 포함 / 시간초과 포함 / 실패·초과 혼합 / 라이프 소진).
  예: 이벤트 40개(발사 10·피버 24·피버종료 6)에서 양쪽 모두 17,475점.
- 조작 차단 6종: 거울 칸 발사, 범위 밖 칸, 음수 시각, 제한시간 초과 시각,
  잘못된 방향, 플레이 중 피버 이벤트 — 전부 거부.
- 기록 코드: 왕복 정상, 점수 위조 시 체크섬 불일치로 거부, 다른 시드 거부, 형식 오류 거부.

### 전체 순위가 주인공입니다

처음엔 결과 화면이 친구 코드 비교를 앞세워, 1:1 비교만 되는 것처럼 보였습니다.
코드 비교는 **서버가 없을 때의 대비책**이지 목적이 아니므로 순서를 뒤집었습니다.

- 우측 상단에 **순위** 버튼을 두어 게임 시작 전에도 언제든 전체 순위를 볼 수 있습니다.
- 게임이 끝나면 **자동으로 등재**되고, 결과 화면에 곧바로
  `LEVEL 4 도달 · 랭킹 644 · 전체 4위`처럼 표시됩니다.
- 순위 패널은 전체 목록이 본문이고, 내 기록 행이 강조됩니다.
- 코드 비교는 `서버 없이 친구와 비교하기` 접이식 항목으로 내렸습니다.
  서버가 연결되지 않았을 때만 자동으로 펼쳐집니다.
- `API_BASE`를 `window.MIRROR_API`로도 지정할 수 있게 해, 파일을 고치지 않고 시험할 수 있습니다.

### 배포 구조 — Cloudflare 한 곳으로

처음엔 "게임은 GitHub Pages, API는 Cloudflare"로 나눠 두었는데,
그러면 계정도 둘, 배포도 둘, CORS 설정까지 필요합니다.
Cloudflare Workers가 정적 파일도 서빙하므로 **한 곳에 같이 올리도록** 바꿨습니다.

- 게임 파일을 `public/`으로 옮기고, Worker가 `/api/*` 외의 요청을 `env.ASSETS`로 넘깁니다.
- `wrangler.toml`을 저장소 최상위로 옮겨 **`wrangler deploy` 한 번**이면 끝납니다.
- 같은 출처가 되므로 **CORS 설정이 필요 없습니다.** `ALLOWED_ORIGINS`는
  게임을 다른 곳에도 올릴 때만 쓰는 선택 항목이 됐습니다.
- `API_BASE`를 손으로 채울 필요가 없어졌습니다. `http(s)`로 열렸으면 같은 출처의 `/api`를
  자동으로 쓰고, `file://`로 열었으면 순위만 꺼집니다.
  `file://`에서는 "파일로 연 상태라 순위를 쓸 수 없다"고 안내하고 코드 비교를 펼칩니다.

### 2단계 — 검증 리더보드 (worker/)

`core.js`를 **그대로 import 하는** Cloudflare Worker + D1을 추가했습니다.
게임은 정적 호스팅 그대로 두고 API만 올립니다.

- `POST /api/run` — 서버가 런을 열고 **시작 시각을 직접 기록**합니다.
- `POST /api/submit` — 입력 로그만 받습니다. **클라이언트 점수는 받지도 않습니다.**
  서버가 `core.replay`로 재생해 점수를 스스로 계산합니다.
- `GET /api/board?seed=` — 시드별 상위 기록.
- 닉네임은 **서버가 생성**합니다(`날카로운반사-800` 형태). 자유 입력을 받지 않는 이유는
  부적절한 표현·개인정보 관리 책임을 지지 않기 위해서입니다.
- `API_BASE`를 비워 두면 순위표만 꺼진 채 게임은 그대로 동작합니다(기본값).

**막는 것 / 못 막는 것을 분명히 해 둡니다.**

리플레이 검증은 "이 시드에서 가능한 수였는가"까지만 증명합니다.
보고된 시각은 검증할 수 없으므로, 런 시작 시각을 서버 시계로 찍어
**보고된 시각의 합보다 실제 경과가 짧으면 거부**하는 방식으로 빈틈을 메웠습니다.
다만 판이 결정론적이라 완전 탐색으로 최적해를 구한 뒤 실제 시간만 흘려보내면
정직한 제출과 구분되지 않습니다. 즉 **불가능한 점수는 막지만, 사람이 아닌 것은 막지 못합니다.**
친구·소규모 커뮤니티 순위표에는 충분하고, 상금이 걸린 랭킹에는 부족합니다.

#### 서버 검증 (Node + 가짜 D1으로 실측)

- 정직한 로그 24개 이벤트 → 최소 필요 경과 56.0초로 계산.
- 즉시 제출 → **거부** (`실제 경과 0초 / 최소 39초`).
- 시간이 흐른 뒤 제출 → **통과**, 서버가 계산한 rank 10,141 · Lv9 · `날카로운반사-800` · 1위.
- 같은 런 재제출 → **거부** (`이미 제출된 런입니다`).
- 조작 로그 → **거부** (`발사 시각 범위 밖`).
- 순위표 조회 정상.

배포 방법과 한계는 [worker/README.md](worker/README.md)에 정리했습니다.

---

## v1.0 — 2026-07-29

남아 있던 두 가지(난이도 고정·라이프 회복 부재)를 해결하고, 버전/업데이트 내역 버튼을 추가했습니다.

### 추가 — 방이 넓어집니다

5×5 안에서는 거울 13개·목표 14칸·6초가 모두 상한이라 **레벨 18에서 난이도가 완전히 고정**됐습니다.
방 크기 자체를 키워 상한을 풀었습니다.

| 레벨 | 방 | 거울 | 목표 칸 | 최소 반사 | 제한시간 |
|---|---|---|---|---|---|
| 1 | 5×5 | 4 | 5 | 1 | 12.6초 |
| 12 | 5×5 | 12 | 11 | 4 | 8.2초 |
| 13 | **6×6** | 13 | 11 | 4 | 10.8초 |
| 24 | 6×6 | 19 | 17 | 6 | 7.5초 |
| 25 | **7×7** | 21 | 17 | 6 | 9초 |
| 35 | 7×7 | 26 | 22 | 6 | 9초 |
| 50 | 7×7 | 26 | 29 | 6 | 9초 |

- 레벨 13부터 6×6, 25부터 7×7입니다. 넓어지는 순간 `6×6` 연출과 함께 알려줍니다.
- 칸 크기는 viewBox에 맞춰 다시 계산하므로 **보드 겉크기는 그대로**입니다.
  선 굵기·화살표·커서는 `--k` 배율(5×5=1, 6×6≈0.82, 7×7≈0.68)로 함께 줄어듭니다.
  세 크기 모두 격자가 viewBox를 오차 없이 채우는 것을 확인했습니다.
- 방이 커지면 기본 시간과 하한도 함께 올라갑니다(6×6은 +3초, 7×7은 +6초).
- 난이도 상한 도달이 **레벨 18 → 50**으로 밀렸습니다. 실측 평균 주 경로도
  6.3칸(Lv1) → 15.9칸(Lv20) → 18.8칸(Lv35)으로 계속 자랍니다.

### 추가 — 라이프 회복

- **피버에서 한 번이라도 득점하면 라이프 +1** (최대 3). 피버는 콤보 5연속을 쌓아야 열리므로
  회복 조건이 이미 실력 게이트에 걸려 있습니다.
- 회복 시 `LIFE +1` 연출과 음성 안내가 나옵니다. 이미 3개면 회복하지 않고,
  피버에서 한 발도 못 맞히면 회복되지 않습니다.

### 추가 — 버전 / 업데이트 내역 버튼

- 우측 상단에 **v1.0** 버튼을 두고, 누르면 버전별 변경 내역이 열립니다(7개 버전 요약).
- 여는 동안 게임은 자동 일시정지되고 `Esc`로 닫히며, 닫으면 포커스가 버튼으로 돌아옵니다.
- 도움말과 같은 패널 로직(`openPanel`/`closePanel`)으로 통합했습니다.

### 변경

- **작은 칸에서의 조준 보완** — 7×7은 320px 화면에서 사분면 높이가 12.2px까지 줄어듭니다.
  누른 채 **보드 바깥으로 밀면** 그 방향으로 조준되도록 했습니다(설치 칸은 유지).
  가장자리 칸에서 바깥으로 튕기는 동작이 그대로 조준이 됩니다.
- 레벨 생성 조건을 완화 시점 450 → 120회, 최대 시도 900 → 420회로 조정했습니다.
  7×7 고레벨에서 판당 **82ms → 27ms(최악)**로 줄었습니다. 이 비용은 레벨 전환 구간에서
  발생하며 플레이어의 제한시간에는 포함되지 않습니다.
- 좁은 화면(≤26rem)에서 조작 버튼의 단축키 표시를 접고 안내 문구를 줄여,
  버전 버튼이 추가된 뒤에도 320×568에서 스크롤이 없습니다.
- HUD 우측에 현재 방 크기를 표시합니다(`5×5 · 최고 N점`).

### 검증

- 콘솔 에러 0건.
- 기하: 5×5 / 6×6 / 7×7 모두 마지막 셀 오른쪽 끝이 정확히 422(=452−30).
- 레벨 생성: 레벨 1~80에서 폴백 **0건**, 최악 27ms.
- 방 확장: Lv1 25칸 → Lv13 36칸(`--k` 0.815, `6×6` 연출) → Lv25 49칸. 7×7에서 발사 182점 득점.
- 라이프: 2개→피버 득점→**3개**(`LIFE +1`), 3개일 때는 유지, 무득점 피버는 회복 없음.
- 버전 패널: 46×28px 버튼, 7개 항목, 열면 `btnLogClose`로 포커스,
  `Esc`로 닫히고 `btnVer`로 포커스 복귀.
- 조준 보완: 7×7 가장자리 칸에서 보드 밖 오른쪽/위/아래로 밀면 각각 E/N/S로 조준,
  설치 칸은 유지되고 떼도 발사되지 않음. 2탭 발사도 7×7에서 정상.
- 320×568에서 가로·세로 스크롤 없음, 텍스트 200% 확대에서 가로 스크롤 없음.

### 남은 사항

- 7×7의 사분면 터치 영역이 320px에서 12.2px입니다. 회전·발사 버튼과 키보드, 그리고
  이번에 추가한 바깥 밀기 조준이 대체 수단이라 WCAG 2.5.8 위반은 아니지만,
  작은 화면에서 정밀 조준은 여전히 버튼 쪽이 편합니다.
- 레벨 50 이후로는 다시 난이도가 고정됩니다(7×7 상한).

---

## v0.9 — 2026-07-29

일반 맵에 **출구 2개**를 도입해 판단 요소를 만들고, 발사를 2탭으로 단축.
피버 점수 표시가 레인을 가리던 문제 수정.

### 추가 — 출구 2개 (일반 맵의 판단 요소)

v0.3부터 지적해 온 "정답 탐색이 단일 전략" 문제를 해결했습니다.

**코인 타일은 답이 아니었습니다.** 거울 반사가 가역적이라 출구 하나당 정답은
'출구에서 역추적한 한 줄'과 그 뒷부분들뿐입니다(측정: 정답의 100%가 퍼펙트 경로의 suffix).
그 한 줄 위에 수집물을 놓으면 경로가 길수록 무조건 많이 먹으므로 판단이 생기지 않습니다.
**분기를 만들려면 줄 자체가 둘이어야 합니다.**

- 매 판 출구가 **2개** 열립니다. 초록 `EXIT`는 ×1, 금색 `EXIT ×2`는 ×2 배점입니다.
- ×2 출구의 경로는 **항상 더 짧게** 생성됩니다. 그래서 매번 `칸수 × 배수`를 비교해야 합니다.
- PERFECT 판정이 "최장 경로"에서 **"이 판에서 가능한 최고 점수"**로 바뀌었습니다.
  긴 쪽을 완주해도 ×2 쪽이 더 높았다면 CLEAR입니다.
- HUD를 `목표 N칸` → `최고 N점`으로 바꿨습니다. 경로 길이를 직접 알려주면 역추적을
  건너뛰게 되므로, 목표 점수만 주고 경로는 직접 찾게 합니다.
- ×2 출구로 나간 빔은 금색으로 그려집니다.
- 색뿐 아니라 글자(`EXIT` / `EXIT ×2`)로도 구분됩니다(WCAG 1.4.1).

두 선택지가 한쪽으로 굳지 않도록, 60% 확률로 두 출구의 점수가 가까워지는 후보를 고릅니다.
그냥 무작위로 두면 고레벨에서 주 경로가 길어져 ×1이 80% 정답이 되어버렸습니다.

실측(각 200판): ×2 유리 / ×1 유리 / 동점 —
Lv1 `145/24/31`, Lv10 `73/76/51`, Lv20 `51/129/20`, Lv30 `46/122/32`.
20점(2칸) 이내 접전이 레벨에 따라 38~72%로, 어느 레벨에서도 답이 고정되지 않습니다.

### 변경 — 발사를 2탭으로

- **같은 타일의 같은 사분면을 한 번 더 누르면 발사**됩니다.
  다른 사분면을 누르면 방향만 바뀌고, 다른 타일을 누르면 설치 위치만 옮겨집니다.
- 회전·발사 버튼과 키보드(`Q`/`E`/`Space`)는 그대로 남아 있습니다.
  버튼까지 손을 옮기지 않고 보드 안에서 끝낼 수 있게 한 것이지, 대체가 아닙니다.
- v0.2처럼 한 번 탭에 바로 나가지 않으므로 오발사로 라이프를 잃는 일은 없습니다.

### 수정 — 피버 점수가 화면을 가리던 문제

피버에서 득점할 때마다 중앙에 44px짜리 `최적!` 플래시가 900ms 동안 떠서
**다음에 어디를 눌러야 할지 레인이 안 보였습니다.**

- 중앙 대형 플래시 대신, 레인 **위쪽 바깥**에 뜨는 작은 점수 표시로 교체했습니다.
  실측 점수 표시 y범위 `106~136`, 레인 y범위 `160~292` — 겹치지 않습니다.
  좌우로도 viewBox 안에 들어오도록 x를 클램프합니다.
- `거울 칸`, `막힘` 안내도 같은 방식으로 바꿨습니다.
- 연출 레이어(`#gFx`)에 `pointer-events:none`을 줘서, 점수가 떠 있는 동안에도
  탭이 타일에 그대로 전달됩니다(레인 위 최상단 요소가 `.fhit`인 것을 확인).

### 검증

- 콘솔 에러 0건.
- 출구 2개: 마커 2개(×2 마커 1개), HUD 최고점 일치.
  실측 판 `6칸×1=60점 vs 4칸×2=80점` → ×2로 쏘면 PERFECT 261점, ×1로 쏘면 CLEAR 151점.
- 2탭 발사: 첫 탭 설치(발사 안 됨) → 다른 사분면 탭은 방향만 변경(발사 안 됨) →
  같은 사분면 재탭에서 발사. 다른 칸 탭은 이동만.
- 레벨 생성: 폴백 0건, 판당 0.1~0.76ms.
- 키보드 전 과정(방향키 → Enter → E → Space) 동작.
- 피버 종료 후 일반 맵 복귀(셀 25개, 출구 2개) 확인.
- 320px에서 가로·세로 스크롤 없음, 모든 버튼 최소 49px.

### 남은 사항

- 레벨 18부터 난이도 고정(거울 13·목표 14칸·6초·최소 반사 4회 모두 상한).
- 라이프 회복 수단 없음.
- 일반 맵 사분면 터치 영역이 320px에서 17.8px(대체 컨트롤이 있어 WCAG 위반은 아님).

---

## v0.8 — 2026-07-29

**v0.4의 레이저 비행 연출이 실제로는 대부분의 환경에서 재생되지 않고 있었습니다.** 원인과 수정.

### 수정 — 연출이 통째로 꺼지던 문제

`fire()`가 `prefers-reduced-motion: reduce`일 때 **연출 전체를 건너뛰도록** 되어 있었습니다.
선단(head)·머즐·충격파·폭발이 하나도 생성되지 않고 빔 선만 정적으로 그려졌습니다.

Windows 11의 **설정 → 접근성 → 시각 효과 → 애니메이션 효과**가 꺼져 있으면
브라우저가 `reduce`를 보고합니다. 이 옵션은 기본으로 꺼진 환경이 흔하고(원격 데스크톱·VM 포함),
그래서 v0.4~v0.7 내내 연출이 안 보였습니다.

**설계가 틀렸습니다.** 레이저 궤적은 장식이 아니라 *빔이 어디로 갔는지 알려주는 핵심 피드백*이라
모션 설정으로 없앨 대상이 아닙니다. 다음과 같이 나눴습니다.

- **궤적 비행은 항상 재생**합니다. 연출을 끄면 사라지는 게 아니라 짧아집니다(일반 360~1300ms → 260ms).
- **장식만 끕니다** — 머즐 플래시, 반사 충격파 링, 출구 폭발링, 실패 파티클,
  화면 흔들림, 피버 펄스, 스파크 확대.

### 추가

- **연출 on/off 버튼** (`F` 키). 초기값은 OS의 `prefers-reduced-motion`을 따르되
  사용자가 언제든 뒤집을 수 있고 `localStorage`(`mirrorrush.fx`)에 유지됩니다.
  OS 설정을 존중하면서 최종 결정권은 사용자에게 두는 방식입니다.
- 장식 애니메이션 제어를 `@media (prefers-reduced-motion)` 대신 `body.nofx` 클래스로 옮겨
  버튼 토글이 CSS 애니메이션까지 함께 제어합니다.

### 변경

- 보조 버튼이 4개(일시정지·소리·연출·도움말)가 되면서 320px에서 세로 스크롤이 생겨,
  좁은 화면(≤26rem)과 터치 기기에서는 단축키 표시를 접도록 했습니다.
  버튼 크기는 68×49px로 유지됩니다(최소 44px).

### 검증 (이번엔 스텁 없이)

v0.4의 검증이 부실했습니다. 미리보기 창이 프레임을 그리지 않아 `requestAnimationFrame`이
멈춰 있었고, 그래서 **프레임을 수동으로 밀어서** 확인했습니다.
그 방식은 `reduce` 분기를 우회해 버려서 "실제로는 연출이 안 나간다"는 사실을 놓쳤습니다.

이번에는 아무것도 스텁하지 않고 이 환경의 실제 설정(`reduce = true`) 그대로 확인했습니다.

- 연출 OFF(OS 기본): 선단이 `146,269 → 146,155 → 121,66`으로 **이동**. 점수·`phase` 정상.
- 연출 ON: 선단이 `386,119 → 346,146 → 282,146 → 224,146 → 153,146 → 103,146 → 28,146`으로
  7개 지점 이동. 반사 지점에서 궤적이 꺾이는 것도 좌표로 확인.
- 다른 판에서는 `303 → 226 → 226 → 300 → 368`로 x가 줄었다 늘어남 — 반사로 진행 방향이 뒤집힌 것.
- 피버에서도 선단 이동 확인(`62 → 23`), 점수 128점 정상 적립.
- 연출 종료 후 머즐·링 잔재 0개.
- 콘솔 에러 0건.
- 320px에서 가로·세로 스크롤 모두 없음, 모든 버튼 최소 44px, 텍스트 200% 확대 시 가로 스크롤 없음.

---

## v0.7 — 2026-07-29

피버 출구를 **매 판 좌·우 중 랜덤 한쪽**으로 변경. (v0.6은 양쪽이 항상 동시에 열려 있었습니다)

### 변경 — 피버 타임

- **출구가 매 판 한쪽에만 열립니다.** 반대쪽 끝은 회색 빗금 **벽**으로 표시됩니다.
  초록 화살표 + `EXIT` 글자 / 회색 빗금 벽으로 모양이 달라 색 없이도 구분됩니다(WCAG 1.4.1).
- 방향 선택이 없는 건 그대로입니다. 타일을 누르면 **그 판의 EXIT 쪽으로 자동 발사**되고,
  에미터 화살표도 그쪽만 가리킵니다. 빔은 1개입니다.
- **출구 방향에 따라 정답이 통째로 뒤집힙니다.** 같은 레인 `· · ╱ · ·` 기준:

  | 선택 | 출구 오른쪽 | 출구 왼쪽 |
  |---|---|---|
  | 0열 | 막힘 | 1칸 |
  | 1열 | 막힘 | **2칸** |
  | 3열 | **2칸** | 막힘 |
  | 4열 | 1칸 | 막힘 |

  거울만 보는 게 아니라 **출구가 어디 열렸는지를 먼저 읽어야** 합니다.
- **최대 칸수가 3칸 → 5칸으로 늘었습니다.** 양쪽 출구일 때는 한쪽 경로가 반토막 났는데,
  한쪽만 열리면서 레인을 끝까지 관통할 수 있게 됐습니다.
  10,000판 실측 분포: 1칸 32.3% · 2칸 24.1% · 3칸 18.6% · 4칸 10.5% · 5칸 14.6%.
- **양쪽 관통 보너스 → `최적!` 보너스로 교체.** 그 레인에서 낼 수 있는 최대 칸수를 뽑으면
  점수 ×1.5와 `최적!` 연출이 붙습니다. 운이 아니라 판단에 보상이 갑니다.
  실측 거울 2열·출구 오른쪽에서 **3열(최적, 2칸) 191점 vs 4열(1칸) 102점 — 1.9배**.
- 거울은 **출구 반대쪽 끝 칸을 항상 비워 두도록** 배치합니다.
  (출구 오른쪽이면 0~3열, 왼쪽이면 1~4열) 어떤 레인이든 반드시 풀립니다.
- 실패해도 라이프·콤보 손실은 없습니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 10,000판: 출구 분포 L 5,058 / R 4,942, 출구쪽 끝칸에 거울 **0건**,
  풀 수 없는 레인 **0건**.
- 판정 정합성 5,000레인 전수 검사: "통과 = 자기와 출구 사이에 거울 없음"과
  경로 칸수(`출구 오른쪽이면 5-c, 왼쪽이면 c+1`) 모두 불일치 **0건**.
- 프레임 수동 구동으로 확인 — EXIT 마커 1개·벽 마커 1개, 빔 1개, 에미터 단방향 화살표,
  최적 위치에서 `최적!` 표시, 출구 반대편 칸은 0점·콤보 유지·라이프 유지,
  거울 없는 레인 5칸 472점, 키보드 `→`+`Space` 동작, 거울 칸 탭 거부.
- 320px 기준 피버 타일 터치 영역 **32.1 × 65.4px** (WCAG 2.5.8 통과).
- EXIT·벽 마커가 viewBox 안에 들어옴 (x 12~427 / 폭 452).
- 회귀: 일반 맵 셀 25개·사분면 84개 그대로, 피버 종료 후 5×5 복귀 확인.

---

## v0.6 — 2026-07-29

피버 레인에 양쪽 출구 도입. 방향 선택을 없애고 **타일 한 번만 누르면** 되도록 단순화.

### 변경 — 피버 타임

- **양쪽 끝이 모두 EXIT**가 됐습니다. 레인이 `[왼쪽 탈출 구역 | 막힌 구간 | 오른쪽 탈출 구역]`으로
  갈려서, 어느 쪽이 뚫려 있고 어느 쪽이 더 긴지를 읽는 판단이 생깁니다.
- **방향을 고를 필요가 없습니다.** 타일을 누르면 그 자리에서 **좌우로 동시에 발사**됩니다.
  탭 한 번에 설치와 발사가 같이 이뤄지고(v0.5는 2탭), 뚫린 쪽 빔이 EXIT로 나갑니다.
  막힌 쪽 빔은 거울에 부딪혀 위·아래로 꺾여 나가는 게 그대로 보입니다.
- **양쪽 관통 보너스** — 거울이 없는 잭팟 레인(약 17%)에서는 양쪽이 다 뚫려 점수 **×1.5**와
  `양쪽 관통!` 연출이 붙습니다. 실측 424점 vs 일반 127점.
- 거울은 이제 **1~3열(안쪽)에만** 놓입니다. 0열은 항상 왼쪽으로, 4열은 항상 오른쪽으로
  나갈 수 있어 어떤 레인이든 반드시 풀립니다. 대신 안전한 끝칸은 1칸짜리 최소 점수입니다.
- 칸당 배점을 `cells×14` → `cells×22`로 올렸습니다. 양쪽 출구가 되며 경로가 짧아진 만큼
  선택 차이를 살리기 위해서입니다. 거울이 2열일 때 **0열(1칸) 121점 vs 1열(2칸) 193점 — 1.6배**.
- 발사 버튼과 `Space`는 커서 위치에서 쏩니다(키보드·버튼 경로 유지). 설치 전에도 활성입니다.
- 실패해도 라이프·콤보 손실은 없습니다.
- 레인 기하를 좌우 대칭으로 재배치했습니다(`FPAD` 18→46, 타일 폭 70.4→64.8).
  EXIT 화살표가 viewBox 안에 들어오는 것을 확인했습니다(왼쪽 16~31, 오른쪽 421~436 / 폭 452).

### 수정

- `flyBeam`이 발사 토큰을 **증가**시키던 것을 **캡처**로 바꿨습니다. 이전에는 두 번째 빔이
  첫 번째 빔의 토큰을 무효화해 좌우 동시 발사가 불가능했습니다.
  이제 무효화는 `stopBeam()`만 담당하고, `fire()` 시작 시 이를 호출합니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 8,000회: 0열·4열에 거울이 놓인 경우 **0건**, 풀 수 없는 레인 **0건**,
  거울 0개 1,343 / 1개 3,361 / 2개 3,296(잭팟 16.8%).
- 판정 정합성 4,000레인 전수 검사: "왼쪽 탈출 = 자기보다 왼쪽에 거울 없음"(우측도 대칭)과
  시뮬레이션 결과 불일치 **0건**.
- 프레임 수동 구동으로 확인 — 탭 즉시 빔 2개 생성, 에미터 양방향 화살표,
  잭팟 시 `양쪽 관통!` 표시, 막힌 칸에서 점수 0·콤보 유지·라이프 유지,
  거울 칸 탭 거부와 안내 문구, 키보드 `←`+`Space` 동작.
- 320px 기준 피버 타일 터치 영역 **32.1 × 65.4px** (WCAG 2.5.8 통과).
- 회귀: 일반 맵 셀 25개·사분면 84개 그대로, 피버 종료 후 5×5 복귀 확인.

---

## v0.5 — 2026-07-29

피버 타임을 1×5 레인 미니게임으로 교체. **일반 5×5 맵은 변경하지 않았습니다.**

### 변경 — 피버 타임

기존 피버는 "아무 타일이나 마구 두드리기"라 판단할 것이 없고 5×5 사분면 조작과도 어긋났습니다.
이제 피버가 시작되면 보드가 **1×5 레인**으로 바뀌고, 출구는 **항상 오른쪽 끝**입니다.

- **타일 선택 → 발사** 2탭 루프. 성공하면 즉시 새 레인이 깔려 6초 동안 반복합니다.
- **거울은 반사판이 아니라 차단벽입니다.** 한 줄이라 거울에 닿은 빔은 위·아래로 꺾이는데
  그 방향이 곧 벽 밖이라 그대로 이탈합니다. 이 성질을 그대로 규칙으로 썼습니다.
  → *가장 오른쪽 거울보다 오른쪽 타일에 설치해야 통과합니다.*
- **왼쪽에서 쏠수록 고득점** — 경로가 길어집니다. 콤보 5 기준 0열 258점 vs 4열 148점(약 1.7배).
  최적해는 "가장 오른쪽 거울 바로 다음 칸"이라, 한눈에 읽고 즉시 누르는 판단 게임이 됩니다.
- 실패해도 **라이프·콤보 손실이 없습니다.** 점수만 못 얻고 다음 레인으로 넘어갑니다.
- 레인은 거울 1~2개를 0~3열에만 배치해 **4열이 항상 비어 있어 반드시 풀립니다.**
  5,000회 생성 검사에서 4열에 거울이 놓인 경우 0건, 풀 수 있는 레인 100%.
- **회전 버튼이 필요 없어졌습니다.** 피버 중에는 발사 방향이 오른쪽으로 고정되므로
  회전 버튼이 자동으로 비활성화되고, 발사 버튼만 살아 있습니다.
- 키보드: `←` `→` 타일 이동 · `Enter` 설치 · `Space` 발사.

**터치 타깃이 크게 좋아졌습니다.** 320px 폭 기준 피버 타일은 **34.9 × 65.4px**로,
같은 화면에서 5×5 사분면 높이 17.8px(WCAG 2.5.8 미달)과 대비됩니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 5,000회: 4열 거울 0건, 거울 1개 2,495 / 2개 2,505, 풀 수 있는 레인 100%.
- 판정 정합성 3,000레인 전수 검사: "가장 오른쪽 거울보다 오른쪽이면 통과"와 시뮬레이션 결과
  불일치 **0건**.
- 프레임 수동 구동으로 전 과정 확인 — 설치 하이라이트, 발사 중 재입력 잠금,
  성공 시 점수·콤보·히트 증가와 새 레인 생성, 실패 시 점수 0·라이프 유지,
  거울 칸 설치 거부, 미설치 발사 시 안내 문구.
- 키보드 `← → Enter Space` 전 과정 동작.
- 회귀: 일반 맵은 셀 25개·사분면 84개 그대로, 레벨 생성의 최소 반사 보장 50/50,
  피버 종료 후 5×5 복귀와 회전 버튼 재활성화 확인.

### 정정

v0.4에서 "1×5는 거울 반사가 성립하지 않아 채택 불가"로 적었는데, 요청의 의도는
**일반 맵이 아니라 피버 타임**이었습니다. 피버에서는 반사가 없다는 성질이 오히려
"거울 = 차단벽"이라는 명확한 규칙이 되어 그대로 채택했습니다.

또 v0.4에 적은 "사분면 탭이 WCAG 2.5.8 미달"은 재검토 결과 **위반이 아닙니다.**
2.5.8에는 같은 화면에 기준을 충족하는 대체 컨트롤이 있으면 예외라는 조항이 있고,
일반 맵에는 48px 회전·발사 버튼과 키보드 조작이 이미 있습니다. 다만 조작감 자체는
개선 여지가 남아 있습니다.

---

## v0.4 — 2026-07-29

레이저 발사 연출 강화.

### 추가

- **레이저 비행 연출** — 기존에는 빔 선이 CSS transition으로 그려지기만 했습니다.
  이제 흰색 **선단(head)**이 경로를 실제로 따라 날아가고, 빔은 그 뒤로 그려집니다.
  `requestAnimationFrame`으로 매 프레임 `getPointAtLength()`를 써서 선단 좌표를 잡기 때문에
  꺾이는 지점에서도 궤적이 정확히 따라갑니다.
  - **머즐 플래시** — 발사 순간 발사구에서 흰 섬광이 터집니다.
  - **반사 충격파** — 거울을 지나는 바로 그 타이밍에 흰 링이 퍼집니다.
    (경로상 거울 수와 링 발생 수가 일치하는 것을 확인)
  - **출구 폭발** — 성공 시 초록 링 3개가 시차를 두고 퍼집니다.
  - **소멸 연출** — 빗나가면 선단이 분홍 파티클 7개로 흩어집니다.
  - 진행 속도를 칸당 68ms → 74ms로 약간 늦춰 궤적이 눈에 들어오게 했습니다.
- 발사 토큰(`S.beamToken`)을 도입해, 연출이 도는 중에 레벨 전환·피버·게임오버가 끼어들면
  즉시 중단하고 결과 처리(`resolve`)가 두 번 불리지 않게 했습니다.
  `nextLevel()` / `startFever()` / `gameOver()`에서 `stopBeam()`을 호출합니다.
- `prefers-reduced-motion: reduce`에서는 연출 전체를 건너뛰고 결과만 즉시 표시합니다.

### 검증

- 콘솔 에러 0건.
- 프레임을 수동 구동해 계측: 선단이 `(306,386) → (271.8,306) → (226,191.8) → (226,31.9)`로
  반사 지점에서 방향을 꺾으며 이동, 반사링 2개 = 경로상 거울 2개 일치,
  출구 폭발링 3개, 완료 후 선단 제거 및 인라인 dash 해제 확인.

### 검토 결과 (구현하지 않음)

- **1×5 한 줄 배치는 거울 반사가 성립하지 않습니다.** 행 수별로 4,000판씩 시뮬레이션한 결과,
  오른쪽 끝으로 나가는 해 중 거울을 거친 비율이 1행에서 **0.0%**(최대 반사 0회, 최대 경로 3칸)였습니다.
  한 줄에서는 거울에 닿는 순간 빔이 위아래로 꺾이는데 그 방향이 곧 벽 밖이라 그대로 이탈합니다.
  2행 36.6%(최대 반사 5회), 3행 48.1%(9회), 5행 59.0%(12회)로, 반사가 살아나려면 최소 2행이 필요합니다.
- **사분면 탭이 좁은 화면에서 WCAG 2.5.8에 미달합니다.** 320px 폭에서 셀이 35.7px로 줄어
  사분면 삼각형 높이가 **17.8px**(기준 24px)가 됩니다. 방향 선택 UI 개편이 필요합니다.

---

## v0.3 — 2026-07-29

버튼 기반 조작 체계 도입, WCAG 2.2 준수, 피버 타임, 레벨 생성 규칙 강화, 효과음 전면 확장.

### 추가

- **설치 → 회전 → 발사 조작 체계** — 탭 한 번에 바로 나가던 방식을 3단계로 분리했습니다.
  - 빈 타일을 탭하면 그 자리에 **레이저를 설치**합니다. 손을 떼도 발사되지 않습니다.
    (누른 채 밀면 v0.2처럼 방향까지 한 번에 지정할 수 있습니다.)
  - `↺ / ↻` **회전 버튼**으로 90°씩 방향을 바꿉니다.
  - `▶ 발사` 버튼으로 쏩니다.
  - 설치된 위치에는 발사구 링과 방향 화살표가 표시됩니다.
- **전체 키보드 조작** (WCAG 2.1.1) — PC에서 버튼을 누르지 않고 플레이할 수 있습니다.
  `방향키` 커서 이동 · `Enter` 설치 · `Q`/`E` 좌우 회전(`R`도 지원) · `Space` 발사 ·
  `P` 일시정지 · `M` 음소거 · `H` 도움말 · `Esc` 닫기. 각 버튼에 단축키를 함께 표기했습니다.
- **FEVER 타임** — 콤보가 5의 배수가 될 때마다 6초간 발동합니다.
  아무 타일이나 연타(또는 `Space` 연타)하면 타격마다 점수와 콤보가 쌓이고,
  타격 지점에 획득 점수가 튀어 오릅니다. 획득량은 `(30 + 타격수×6) × 콤보배수`로 계속 커집니다.
- **일시정지** (WCAG 2.2.2) — `P` 또는 버튼. 남은 시간이 그대로 보존됩니다.
- **도움말 오버레이** — `H`로 열고 `Esc`로 닫습니다(WCAG 1.4.13). 여는 순간 자동으로 일시정지합니다.
- **소리 켜기/끄기** — `M` 또는 버튼. 설정이 `localStorage`에 유지됩니다.

### WCAG 2.2 적용

- **1.4.3 색상 대비 (AA)** — 배경 대비를 계산해 팔레트를 재조정했습니다.
  보조 텍스트 `--dim`을 `#6b7299`(3.7:1, 미달) → `#8b93bd`(**5.7:1**)로,
  `--pink` `#ff3d8b`(5.1:1) → `#ff6ba6`(**6.5:1**), `--green`은 **13.4:1**,
  본문 **14.6:1**, 오버레이 본문 **12.5:1**. 판독 불가였던 푸터 색(`#454b70`)도 교체했습니다.
- **1.4.11 비텍스트 대비 (AA)** — 셀·패널 테두리 `--line`을 `#1f2340`(1.3:1, 미달) →
  `#565f96`(**3.0:1**)로 올려 타일 경계가 식별되게 했습니다.
- **1.4.1 색에 의존하지 않기 (A)** — 실패 빔은 분홍색뿐 아니라 **점선**으로,
  시간 경고 바는 색뿐 아니라 **사선 패턴 + 남은 초 텍스트**로, 출구는 초록 화살표에
  **EXIT 글자**를 덧붙여 색 없이도 구분됩니다.
- **1.4.4 텍스트 크기 조정 (AA)** — 모든 치수를 `px` → `rem`으로 전환. 200% 확대에서
  가로 스크롤 없이 세로로 리플로우되는 것을 확인했습니다.
- **1.4.10 리플로우 (AA)** — 320px 폭에서 가로 스크롤 0.
- **1.4.13 호버·포커스 콘텐츠 (AA)** — 도움말 오버레이가 `Esc`로 닫힙니다.
- **2.1.1 키보드 (A)** / **2.5.7 드래그 대안 (AA)** — v0.2의 드래그 조준이 유일한 입력이었던 것을
  버튼·키보드로 완전히 대체 가능하게 했습니다.
- **2.4.7 포커스 표시 (AA)** — 모든 조작 요소에 흰색 3px `:focus-visible` 아웃라인.
- **2.5.8 터치 타깃 (AA)** — 모든 버튼 최소 44px 높이(측정값 48px, 기준 24px 초과).
- **2.2.2 일시정지 (A)** — 제한시간·애니메이션을 멈출 수 있습니다.
- **3.3.1 오류 식별 (A)** — 실패 사유를 `거울 사이를 무한히 맴돌았습니다` /
  `초록 EXIT가 아닌 곳으로 나갔습니다`처럼 구체적으로 표시합니다.
  레이저 없이 발사를 누르면 `먼저 타일을 눌러 레이저를 설치하세요`를 안내합니다.
- **4.1.2 이름·역할·값 (A)** — 조작부를 전부 `<button>`으로 구성하고 `aria-label`,
  `aria-pressed`, `aria-disabled`를 붙였습니다. 보드는 `role="application"`,
  타임바는 `role="progressbar"`.
- **4.1.3 상태 메시지 (AA)** — `aria-live` 영역에 레벨 시작, 설치 위치와 방향, 회전 방향,
  클리어·실패 사유, 피버, 게임 오버를 음성으로 안내합니다.
- `prefers-reduced-motion`에서 흔들림·플래시·스파크·빔 애니메이션을 제거합니다.

### 변경

- **레벨 생성기: 직선 정답 차단** — 거울을 하나도 거치지 않고 그냥 직선으로 쏘면 되는 판이
  나오던 문제를 막았습니다. 두 조건을 추가했습니다.
  1. 출구가 걸린 행/열에 거울이 최소 1개 (요청하신 규칙 그대로).
  2. 정답 경로의 반사 횟수가 최소 `1 + ⌊레벨/4⌋`회(최대 4회).

  레벨 1~30에서 각 40판씩 검사해 **직선 정답 0건**, 출구 행/열에 거울이 없는 판 **0건**,
  실측 최소 반사 횟수는 레벨 1에서 1회, 레벨 20 이상에서 8회였습니다.
  생성 비용은 판당 0.08~0.83ms로 이전과 같은 수준입니다.
- 안전망(생성 실패 시 폴백)도 거울 1개를 포함한 배치로 교체했습니다. 실측 도달 0회.
- 실패 시 설치된 레이저를 해제해 다음 판이 깨끗하게 시작됩니다.

### 효과음

18종으로 확장했습니다. 마스터 게인과 밴드패스 노이즈 제너레이터를 추가해,
기존 사각파 단음 위주에서 벗어나 톤 + 노이즈 조합으로 만듭니다.

설치 · 회전 · 발사(하강 스윕+노이즈) · 경로 이동음 · **거울에 닿을 때마다 반사음(음정 상승)** ·
출구 통과 · 클리어 · 퍼펙트 팡파레 · 미스 · 시간 초과 · 카운트다운 틱(마지막 3초) ·
레벨업 · 피버 진입 · 피버 타격(음정 상승) · 피버 종료 · 게임 오버 · 일시정지 · 재개 · 버튼 · 커서 이동.

### 수정

- **피버 종료 후 900ms 동안 점수가 계속 들어가던 버그** — `endFever()`가 `S.phase`를 바꾸지 않아,
  게이지가 0이 된 뒤 다음 레벨이 시작될 때까지 연타가 그대로 득점됐습니다.
  실측으로 확인 후 수정했고, 지금은 종료 직후 10회 연타에도 획득 점수 0입니다.
- **실패 빔의 점선 패턴이 보이지 않던 문제** — 진행 애니메이션용 인라인 `stroke-dasharray`가
  CSS 점선을 덮고 있었습니다. 애니메이션이 끝나면 인라인 값을 걷어내도록 수정했습니다.
- 키 핸들러의 `e.target.closest` 호출에 방어 코드를 추가했습니다(target이 Element가 아닐 때).

### 검증

- 콘솔 에러 0건.
- 키보드만으로 `방향키 이동 → Enter 설치 → E 회전 → Space 발사 → CLEAR` 전 과정 재현.
- 탭 설치 후 발사되지 않음, 버튼 회전, 버튼 발사, 미설치 발사 안내 확인.
- 콤보 5에서 피버 진입 → `Space` 8연타로 1,144점·8히트·콤보 13 확인 → 종료 후 무득점 확인.
- 일시정지 중 남은 시간 보존, 도움말 `Esc` 닫힘, 소리 토글 확인.
- 320×568 / 390×844 / 텍스트 200% 확대 모두 가로 스크롤 0.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초, 최소 반사 4회가
  모두 상한에 닿습니다.
- **정답 탐색은 여전히 단일 전략입니다.** 거울 반사가 가역적이라 모든 정답이 퍼펙트 경로의
  뒷부분이고, 퍼펙트 정답은 항상 1개뿐입니다. 코인 타일이나 출구 2개 같은 기믹이 있어야
  `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).

---

## v0.2 — 2026-07-29

모바일 조작성 개선과 코드 리뷰에서 나온 버그 수정.

### 추가

- **드래그 조준 입력** — 빈 칸을 누른 채 원하는 방향으로 밀면 그 방향이 조준되고, 손을 떼는 순간 발사됩니다.
  누르는 동안 조준된 사분면이 청록색으로 채워지고 화살표가 밝아져, 쏘기 전에 방향을 확인·수정할 수 있습니다.
  보드 바깥에서 손을 떼면 발사가 취소됩니다.
- **터치 환경 방향 가이드** — `@media (hover:none)`에서 빈 칸마다 4방향 화살표를 옅게(opacity .12) 상시 표시합니다.
- **최고 점수 저장** — `localStorage`(`mirrorrush.best`)에 기록합니다. 시작 화면과 GAME OVER 화면에 `BEST`를 표시하고,
  경신 시 `★ 신기록!`을 띄웁니다. localStorage 접근이 차단된 환경에서도 조용히 동작합니다.
- **좁은 화면 대응** — 세로 620px 이하(가로모드 등)에서 HUD를 압축하고 푸터를 숨깁니다.
- 노치 대응(`env(safe-area-inset-*)`), `prefers-reduced-motion` 존중, 버튼 최소 터치 영역 44px.

### 변경

- 보드 크기를 뷰포트 높이 기준으로도 제한(`min(470px, max(230px, 100dvh - 250px))`).
  320×568부터 가로모드까지 스크롤 없이 한 화면에 들어옵니다.
- HUD 라벨 `최대 N칸` → `목표 N칸`. 자기 기록으로 오해하기 쉬웠습니다.
- 클리어 플래시에 실제 적용된 콤보 배수를 표시(`6칸 · x1.15 · +221`).
  HUD의 COMBO는 *다음* 판 배수라 획득 점수와 계산이 맞지 않아 보였습니다.
- 조준 시 짧은 클릭음 추가.

### 수정

- **`requestAnimationFrame` 루프 누수** — `nextLevel()`이 `stopTimer()` 없이 `tick()`을 호출해,
  `startGame()`이 재진입하면 이전 타이머 루프가 고아가 된 채 영구히 돌았습니다.
  실측 시 `stopTimer()` 호출 후에도 600ms 동안 6프레임이 계속 갱신됐고, 수정 후 0프레임입니다.
  `nextLevel()`에 `stopTimer()`를 추가하고 `startGame()`에 재진입 가드를 넣었습니다.
- **확대 차단 해제 (접근성)** — 뷰포트의 `maximum-scale=1, user-scalable=no`를 제거했습니다(WCAG 1.4.4).
  더블탭 줌은 `touch-action`이 이미 막고 있습니다.
- 발사·시간초과·게임오버 시 조준 상태를 정리하도록 수정.
- 데드코드였던 `.locked` CSS 규칙과 클래스 토글 제거. `fire()`가 이미 `gQuads`를 비웁니다.
- `.cellbg`의 중복 `rx` 선언 정리.

### 검증

- 콘솔 에러 0건.
- 레벨 생성 성능: 레벨 1~30에서 판당 0.1~0.7ms. 800회 시도 안전망(빈 그리드 폴백) 도달 0회.
- 드래그 조준을 PointerEvent로 재현해 `누름 → 방향 전환 → 발사 → CLEAR`까지 확인.
- 뷰포트 320×568 / 390×844 / 844×390(가로)에서 레이아웃 확인.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 완전히 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초가 모두 상한에 닿아
  이후로는 무한 반복 구간입니다.
- **정답 탐색이 단일 전략입니다.** 거울 반사가 가역적이라, 정답 530개를 검증한 결과
  **모든 정답이 퍼펙트 경로의 뒷부분(suffix)이고 퍼펙트 정답은 항상 정확히 1개**였습니다.
  즉 "출구에서 역추적" 외에 판단할 것이 없어, 퍼펙트 2배 보너스가 사실상 이진 판정으로 작동합니다.
  코인 타일·출구 2개·반사 횟수 제한 같은 기믹을 넣으면 `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).
- 키보드 플레이 미지원(SPACE 재시작만).

---

## v0.1 — 최초 버전

- 5×5 거울 방 레이저 퍼즐. 빈 칸의 사분면을 클릭해 해당 방향으로 발사, 초록 출구로 탈출하면 클리어.
- 전수 시뮬레이션 기반 레벨 생성기 — 풀 수 있는 판만 출제하고 해의 개수와 최장 경로 길이를 제어.
- 레벨·점수·콤보·라이프 HUD, 제한시간 바, SVG 빔 애니메이션, WebAudio 효과음.
- 의존성 없는 단일 HTML 파일.
