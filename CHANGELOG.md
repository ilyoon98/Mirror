# 버전 로그 — MIRROR RUSH

이 파일은 릴리스마다 갱신합니다. 최신 버전이 맨 위입니다.

---

## v0.3 — 2026-07-29

버튼 기반 조작 체계 도입, WCAG 2.2 준수, 피버 타임, 레벨 생성 규칙 강화, 효과음 전면 확장.

### 추가

- **설치 → 회전 → 발사 조작 체계** — 탭 한 번에 바로 나가던 방식을 3단계로 분리했습니다.
  - 빈 타일을 탭하면 그 자리에 **레이저를 설치**합니다. 손을 떼도 발사되지 않습니다.
    (누른 채 밀면 v0.2처럼 방향까지 한 번에 지정할 수 있습니다.)
  - `↺ / ↻` **회전 버튼**으로 90°씩 방향을 바꿉니다.
  - `▶ 발사` 버튼으로 쏩니다.
  - 설치된 위치에는 발사구 링과 방향 화살표가 표시됩니다.
- **전체 키보드 조작** (WCAG 2.1.1) — PC에서 버튼을 누르지 않고 플레이할 수 있습니다.
  `방향키` 커서 이동 · `Enter` 설치 · `Q`/`E` 좌우 회전(`R`도 지원) · `Space` 발사 ·
  `P` 일시정지 · `M` 음소거 · `H` 도움말 · `Esc` 닫기. 각 버튼에 단축키를 함께 표기했습니다.
- **FEVER 타임** — 콤보가 5의 배수가 될 때마다 6초간 발동합니다.
  아무 타일이나 연타(또는 `Space` 연타)하면 타격마다 점수와 콤보가 쌓이고,
  타격 지점에 획득 점수가 튀어 오릅니다. 획득량은 `(30 + 타격수×6) × 콤보배수`로 계속 커집니다.
- **일시정지** (WCAG 2.2.2) — `P` 또는 버튼. 남은 시간이 그대로 보존됩니다.
- **도움말 오버레이** — `H`로 열고 `Esc`로 닫습니다(WCAG 1.4.13). 여는 순간 자동으로 일시정지합니다.
- **소리 켜기/끄기** — `M` 또는 버튼. 설정이 `localStorage`에 유지됩니다.

### WCAG 2.2 적용

- **1.4.3 색상 대비 (AA)** — 배경 대비를 계산해 팔레트를 재조정했습니다.
  보조 텍스트 `--dim`을 `#6b7299`(3.7:1, 미달) → `#8b93bd`(**5.7:1**)로,
  `--pink` `#ff3d8b`(5.1:1) → `#ff6ba6`(**6.5:1**), `--green`은 **13.4:1**,
  본문 **14.6:1**, 오버레이 본문 **12.5:1**. 판독 불가였던 푸터 색(`#454b70`)도 교체했습니다.
- **1.4.11 비텍스트 대비 (AA)** — 셀·패널 테두리 `--line`을 `#1f2340`(1.3:1, 미달) →
  `#565f96`(**3.0:1**)로 올려 타일 경계가 식별되게 했습니다.
- **1.4.1 색에 의존하지 않기 (A)** — 실패 빔은 분홍색뿐 아니라 **점선**으로,
  시간 경고 바는 색뿐 아니라 **사선 패턴 + 남은 초 텍스트**로, 출구는 초록 화살표에
  **EXIT 글자**를 덧붙여 색 없이도 구분됩니다.
- **1.4.4 텍스트 크기 조정 (AA)** — 모든 치수를 `px` → `rem`으로 전환. 200% 확대에서
  가로 스크롤 없이 세로로 리플로우되는 것을 확인했습니다.
- **1.4.10 리플로우 (AA)** — 320px 폭에서 가로 스크롤 0.
- **1.4.13 호버·포커스 콘텐츠 (AA)** — 도움말 오버레이가 `Esc`로 닫힙니다.
- **2.1.1 키보드 (A)** / **2.5.7 드래그 대안 (AA)** — v0.2의 드래그 조준이 유일한 입력이었던 것을
  버튼·키보드로 완전히 대체 가능하게 했습니다.
- **2.4.7 포커스 표시 (AA)** — 모든 조작 요소에 흰색 3px `:focus-visible` 아웃라인.
- **2.5.8 터치 타깃 (AA)** — 모든 버튼 최소 44px 높이(측정값 48px, 기준 24px 초과).
- **2.2.2 일시정지 (A)** — 제한시간·애니메이션을 멈출 수 있습니다.
- **3.3.1 오류 식별 (A)** — 실패 사유를 `거울 사이를 무한히 맴돌았습니다` /
  `초록 EXIT가 아닌 곳으로 나갔습니다`처럼 구체적으로 표시합니다.
  레이저 없이 발사를 누르면 `먼저 타일을 눌러 레이저를 설치하세요`를 안내합니다.
- **4.1.2 이름·역할·값 (A)** — 조작부를 전부 `<button>`으로 구성하고 `aria-label`,
  `aria-pressed`, `aria-disabled`를 붙였습니다. 보드는 `role="application"`,
  타임바는 `role="progressbar"`.
- **4.1.3 상태 메시지 (AA)** — `aria-live` 영역에 레벨 시작, 설치 위치와 방향, 회전 방향,
  클리어·실패 사유, 피버, 게임 오버를 음성으로 안내합니다.
- `prefers-reduced-motion`에서 흔들림·플래시·스파크·빔 애니메이션을 제거합니다.

### 변경

- **레벨 생성기: 직선 정답 차단** — 거울을 하나도 거치지 않고 그냥 직선으로 쏘면 되는 판이
  나오던 문제를 막았습니다. 두 조건을 추가했습니다.
  1. 출구가 걸린 행/열에 거울이 최소 1개 (요청하신 규칙 그대로).
  2. 정답 경로의 반사 횟수가 최소 `1 + ⌊레벨/4⌋`회(최대 4회).

  레벨 1~30에서 각 40판씩 검사해 **직선 정답 0건**, 출구 행/열에 거울이 없는 판 **0건**,
  실측 최소 반사 횟수는 레벨 1에서 1회, 레벨 20 이상에서 8회였습니다.
  생성 비용은 판당 0.08~0.83ms로 이전과 같은 수준입니다.
- 안전망(생성 실패 시 폴백)도 거울 1개를 포함한 배치로 교체했습니다. 실측 도달 0회.
- 실패 시 설치된 레이저를 해제해 다음 판이 깨끗하게 시작됩니다.

### 효과음

18종으로 확장했습니다. 마스터 게인과 밴드패스 노이즈 제너레이터를 추가해,
기존 사각파 단음 위주에서 벗어나 톤 + 노이즈 조합으로 만듭니다.

설치 · 회전 · 발사(하강 스윕+노이즈) · 경로 이동음 · **거울에 닿을 때마다 반사음(음정 상승)** ·
출구 통과 · 클리어 · 퍼펙트 팡파레 · 미스 · 시간 초과 · 카운트다운 틱(마지막 3초) ·
레벨업 · 피버 진입 · 피버 타격(음정 상승) · 피버 종료 · 게임 오버 · 일시정지 · 재개 · 버튼 · 커서 이동.

### 수정

- **피버 종료 후 900ms 동안 점수가 계속 들어가던 버그** — `endFever()`가 `S.phase`를 바꾸지 않아,
  게이지가 0이 된 뒤 다음 레벨이 시작될 때까지 연타가 그대로 득점됐습니다.
  실측으로 확인 후 수정했고, 지금은 종료 직후 10회 연타에도 획득 점수 0입니다.
- **실패 빔의 점선 패턴이 보이지 않던 문제** — 진행 애니메이션용 인라인 `stroke-dasharray`가
  CSS 점선을 덮고 있었습니다. 애니메이션이 끝나면 인라인 값을 걷어내도록 수정했습니다.
- 키 핸들러의 `e.target.closest` 호출에 방어 코드를 추가했습니다(target이 Element가 아닐 때).

### 검증

- 콘솔 에러 0건.
- 키보드만으로 `방향키 이동 → Enter 설치 → E 회전 → Space 발사 → CLEAR` 전 과정 재현.
- 탭 설치 후 발사되지 않음, 버튼 회전, 버튼 발사, 미설치 발사 안내 확인.
- 콤보 5에서 피버 진입 → `Space` 8연타로 1,144점·8히트·콤보 13 확인 → 종료 후 무득점 확인.
- 일시정지 중 남은 시간 보존, 도움말 `Esc` 닫힘, 소리 토글 확인.
- 320×568 / 390×844 / 텍스트 200% 확대 모두 가로 스크롤 0.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초, 최소 반사 4회가
  모두 상한에 닿습니다.
- **정답 탐색은 여전히 단일 전략입니다.** 거울 반사가 가역적이라 모든 정답이 퍼펙트 경로의
  뒷부분이고, 퍼펙트 정답은 항상 1개뿐입니다. 코인 타일이나 출구 2개 같은 기믹이 있어야
  `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).

---

## v0.2 — 2026-07-29

모바일 조작성 개선과 코드 리뷰에서 나온 버그 수정.

### 추가

- **드래그 조준 입력** — 빈 칸을 누른 채 원하는 방향으로 밀면 그 방향이 조준되고, 손을 떼는 순간 발사됩니다.
  누르는 동안 조준된 사분면이 청록색으로 채워지고 화살표가 밝아져, 쏘기 전에 방향을 확인·수정할 수 있습니다.
  보드 바깥에서 손을 떼면 발사가 취소됩니다.
- **터치 환경 방향 가이드** — `@media (hover:none)`에서 빈 칸마다 4방향 화살표를 옅게(opacity .12) 상시 표시합니다.
- **최고 점수 저장** — `localStorage`(`mirrorrush.best`)에 기록합니다. 시작 화면과 GAME OVER 화면에 `BEST`를 표시하고,
  경신 시 `★ 신기록!`을 띄웁니다. localStorage 접근이 차단된 환경에서도 조용히 동작합니다.
- **좁은 화면 대응** — 세로 620px 이하(가로모드 등)에서 HUD를 압축하고 푸터를 숨깁니다.
- 노치 대응(`env(safe-area-inset-*)`), `prefers-reduced-motion` 존중, 버튼 최소 터치 영역 44px.

### 변경

- 보드 크기를 뷰포트 높이 기준으로도 제한(`min(470px, max(230px, 100dvh - 250px))`).
  320×568부터 가로모드까지 스크롤 없이 한 화면에 들어옵니다.
- HUD 라벨 `최대 N칸` → `목표 N칸`. 자기 기록으로 오해하기 쉬웠습니다.
- 클리어 플래시에 실제 적용된 콤보 배수를 표시(`6칸 · x1.15 · +221`).
  HUD의 COMBO는 *다음* 판 배수라 획득 점수와 계산이 맞지 않아 보였습니다.
- 조준 시 짧은 클릭음 추가.

### 수정

- **`requestAnimationFrame` 루프 누수** — `nextLevel()`이 `stopTimer()` 없이 `tick()`을 호출해,
  `startGame()`이 재진입하면 이전 타이머 루프가 고아가 된 채 영구히 돌았습니다.
  실측 시 `stopTimer()` 호출 후에도 600ms 동안 6프레임이 계속 갱신됐고, 수정 후 0프레임입니다.
  `nextLevel()`에 `stopTimer()`를 추가하고 `startGame()`에 재진입 가드를 넣었습니다.
- **확대 차단 해제 (접근성)** — 뷰포트의 `maximum-scale=1, user-scalable=no`를 제거했습니다(WCAG 1.4.4).
  더블탭 줌은 `touch-action`이 이미 막고 있습니다.
- 발사·시간초과·게임오버 시 조준 상태를 정리하도록 수정.
- 데드코드였던 `.locked` CSS 규칙과 클래스 토글 제거. `fire()`가 이미 `gQuads`를 비웁니다.
- `.cellbg`의 중복 `rx` 선언 정리.

### 검증

- 콘솔 에러 0건.
- 레벨 생성 성능: 레벨 1~30에서 판당 0.1~0.7ms. 800회 시도 안전망(빈 그리드 폴백) 도달 0회.
- 드래그 조준을 PointerEvent로 재현해 `누름 → 방향 전환 → 발사 → CLEAR`까지 확인.
- 뷰포트 320×568 / 390×844 / 844×390(가로)에서 레이아웃 확인.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 완전히 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초가 모두 상한에 닿아
  이후로는 무한 반복 구간입니다.
- **정답 탐색이 단일 전략입니다.** 거울 반사가 가역적이라, 정답 530개를 검증한 결과
  **모든 정답이 퍼펙트 경로의 뒷부분(suffix)이고 퍼펙트 정답은 항상 정확히 1개**였습니다.
  즉 "출구에서 역추적" 외에 판단할 것이 없어, 퍼펙트 2배 보너스가 사실상 이진 판정으로 작동합니다.
  코인 타일·출구 2개·반사 횟수 제한 같은 기믹을 넣으면 `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).
- 키보드 플레이 미지원(SPACE 재시작만).

---

## v0.1 — 최초 버전

- 5×5 거울 방 레이저 퍼즐. 빈 칸의 사분면을 클릭해 해당 방향으로 발사, 초록 출구로 탈출하면 클리어.
- 전수 시뮬레이션 기반 레벨 생성기 — 풀 수 있는 판만 출제하고 해의 개수와 최장 경로 길이를 제어.
- 레벨·점수·콤보·라이프 HUD, 제한시간 바, SVG 빔 애니메이션, WebAudio 효과음.
- 의존성 없는 단일 HTML 파일.
