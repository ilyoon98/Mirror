# 버전 로그 — MIRROR RUSH

이 파일은 릴리스마다 갱신합니다. 최신 버전이 맨 위입니다.

---

## v0.9 — 2026-07-29

일반 맵에 **출구 2개**를 도입해 판단 요소를 만들고, 발사를 2탭으로 단축.
피버 점수 표시가 레인을 가리던 문제 수정.

### 추가 — 출구 2개 (일반 맵의 판단 요소)

v0.3부터 지적해 온 "정답 탐색이 단일 전략" 문제를 해결했습니다.

**코인 타일은 답이 아니었습니다.** 거울 반사가 가역적이라 출구 하나당 정답은
'출구에서 역추적한 한 줄'과 그 뒷부분들뿐입니다(측정: 정답의 100%가 퍼펙트 경로의 suffix).
그 한 줄 위에 수집물을 놓으면 경로가 길수록 무조건 많이 먹으므로 판단이 생기지 않습니다.
**분기를 만들려면 줄 자체가 둘이어야 합니다.**

- 매 판 출구가 **2개** 열립니다. 초록 `EXIT`는 ×1, 금색 `EXIT ×2`는 ×2 배점입니다.
- ×2 출구의 경로는 **항상 더 짧게** 생성됩니다. 그래서 매번 `칸수 × 배수`를 비교해야 합니다.
- PERFECT 판정이 "최장 경로"에서 **"이 판에서 가능한 최고 점수"**로 바뀌었습니다.
  긴 쪽을 완주해도 ×2 쪽이 더 높았다면 CLEAR입니다.
- HUD를 `목표 N칸` → `최고 N점`으로 바꿨습니다. 경로 길이를 직접 알려주면 역추적을
  건너뛰게 되므로, 목표 점수만 주고 경로는 직접 찾게 합니다.
- ×2 출구로 나간 빔은 금색으로 그려집니다.
- 색뿐 아니라 글자(`EXIT` / `EXIT ×2`)로도 구분됩니다(WCAG 1.4.1).

두 선택지가 한쪽으로 굳지 않도록, 60% 확률로 두 출구의 점수가 가까워지는 후보를 고릅니다.
그냥 무작위로 두면 고레벨에서 주 경로가 길어져 ×1이 80% 정답이 되어버렸습니다.

실측(각 200판): ×2 유리 / ×1 유리 / 동점 —
Lv1 `145/24/31`, Lv10 `73/76/51`, Lv20 `51/129/20`, Lv30 `46/122/32`.
20점(2칸) 이내 접전이 레벨에 따라 38~72%로, 어느 레벨에서도 답이 고정되지 않습니다.

### 변경 — 발사를 2탭으로

- **같은 타일의 같은 사분면을 한 번 더 누르면 발사**됩니다.
  다른 사분면을 누르면 방향만 바뀌고, 다른 타일을 누르면 설치 위치만 옮겨집니다.
- 회전·발사 버튼과 키보드(`Q`/`E`/`Space`)는 그대로 남아 있습니다.
  버튼까지 손을 옮기지 않고 보드 안에서 끝낼 수 있게 한 것이지, 대체가 아닙니다.
- v0.2처럼 한 번 탭에 바로 나가지 않으므로 오발사로 라이프를 잃는 일은 없습니다.

### 수정 — 피버 점수가 화면을 가리던 문제

피버에서 득점할 때마다 중앙에 44px짜리 `최적!` 플래시가 900ms 동안 떠서
**다음에 어디를 눌러야 할지 레인이 안 보였습니다.**

- 중앙 대형 플래시 대신, 레인 **위쪽 바깥**에 뜨는 작은 점수 표시로 교체했습니다.
  실측 점수 표시 y범위 `106~136`, 레인 y범위 `160~292` — 겹치지 않습니다.
  좌우로도 viewBox 안에 들어오도록 x를 클램프합니다.
- `거울 칸`, `막힘` 안내도 같은 방식으로 바꿨습니다.
- 연출 레이어(`#gFx`)에 `pointer-events:none`을 줘서, 점수가 떠 있는 동안에도
  탭이 타일에 그대로 전달됩니다(레인 위 최상단 요소가 `.fhit`인 것을 확인).

### 검증

- 콘솔 에러 0건.
- 출구 2개: 마커 2개(×2 마커 1개), HUD 최고점 일치.
  실측 판 `6칸×1=60점 vs 4칸×2=80점` → ×2로 쏘면 PERFECT 261점, ×1로 쏘면 CLEAR 151점.
- 2탭 발사: 첫 탭 설치(발사 안 됨) → 다른 사분면 탭은 방향만 변경(발사 안 됨) →
  같은 사분면 재탭에서 발사. 다른 칸 탭은 이동만.
- 레벨 생성: 폴백 0건, 판당 0.1~0.76ms.
- 키보드 전 과정(방향키 → Enter → E → Space) 동작.
- 피버 종료 후 일반 맵 복귀(셀 25개, 출구 2개) 확인.
- 320px에서 가로·세로 스크롤 없음, 모든 버튼 최소 49px.

### 남은 사항

- 레벨 18부터 난이도 고정(거울 13·목표 14칸·6초·최소 반사 4회 모두 상한).
- 라이프 회복 수단 없음.
- 일반 맵 사분면 터치 영역이 320px에서 17.8px(대체 컨트롤이 있어 WCAG 위반은 아님).

---

## v0.8 — 2026-07-29

**v0.4의 레이저 비행 연출이 실제로는 대부분의 환경에서 재생되지 않고 있었습니다.** 원인과 수정.

### 수정 — 연출이 통째로 꺼지던 문제

`fire()`가 `prefers-reduced-motion: reduce`일 때 **연출 전체를 건너뛰도록** 되어 있었습니다.
선단(head)·머즐·충격파·폭발이 하나도 생성되지 않고 빔 선만 정적으로 그려졌습니다.

Windows 11의 **설정 → 접근성 → 시각 효과 → 애니메이션 효과**가 꺼져 있으면
브라우저가 `reduce`를 보고합니다. 이 옵션은 기본으로 꺼진 환경이 흔하고(원격 데스크톱·VM 포함),
그래서 v0.4~v0.7 내내 연출이 안 보였습니다.

**설계가 틀렸습니다.** 레이저 궤적은 장식이 아니라 *빔이 어디로 갔는지 알려주는 핵심 피드백*이라
모션 설정으로 없앨 대상이 아닙니다. 다음과 같이 나눴습니다.

- **궤적 비행은 항상 재생**합니다. 연출을 끄면 사라지는 게 아니라 짧아집니다(일반 360~1300ms → 260ms).
- **장식만 끕니다** — 머즐 플래시, 반사 충격파 링, 출구 폭발링, 실패 파티클,
  화면 흔들림, 피버 펄스, 스파크 확대.

### 추가

- **연출 on/off 버튼** (`F` 키). 초기값은 OS의 `prefers-reduced-motion`을 따르되
  사용자가 언제든 뒤집을 수 있고 `localStorage`(`mirrorrush.fx`)에 유지됩니다.
  OS 설정을 존중하면서 최종 결정권은 사용자에게 두는 방식입니다.
- 장식 애니메이션 제어를 `@media (prefers-reduced-motion)` 대신 `body.nofx` 클래스로 옮겨
  버튼 토글이 CSS 애니메이션까지 함께 제어합니다.

### 변경

- 보조 버튼이 4개(일시정지·소리·연출·도움말)가 되면서 320px에서 세로 스크롤이 생겨,
  좁은 화면(≤26rem)과 터치 기기에서는 단축키 표시를 접도록 했습니다.
  버튼 크기는 68×49px로 유지됩니다(최소 44px).

### 검증 (이번엔 스텁 없이)

v0.4의 검증이 부실했습니다. 미리보기 창이 프레임을 그리지 않아 `requestAnimationFrame`이
멈춰 있었고, 그래서 **프레임을 수동으로 밀어서** 확인했습니다.
그 방식은 `reduce` 분기를 우회해 버려서 "실제로는 연출이 안 나간다"는 사실을 놓쳤습니다.

이번에는 아무것도 스텁하지 않고 이 환경의 실제 설정(`reduce = true`) 그대로 확인했습니다.

- 연출 OFF(OS 기본): 선단이 `146,269 → 146,155 → 121,66`으로 **이동**. 점수·`phase` 정상.
- 연출 ON: 선단이 `386,119 → 346,146 → 282,146 → 224,146 → 153,146 → 103,146 → 28,146`으로
  7개 지점 이동. 반사 지점에서 궤적이 꺾이는 것도 좌표로 확인.
- 다른 판에서는 `303 → 226 → 226 → 300 → 368`로 x가 줄었다 늘어남 — 반사로 진행 방향이 뒤집힌 것.
- 피버에서도 선단 이동 확인(`62 → 23`), 점수 128점 정상 적립.
- 연출 종료 후 머즐·링 잔재 0개.
- 콘솔 에러 0건.
- 320px에서 가로·세로 스크롤 모두 없음, 모든 버튼 최소 44px, 텍스트 200% 확대 시 가로 스크롤 없음.

---

## v0.7 — 2026-07-29

피버 출구를 **매 판 좌·우 중 랜덤 한쪽**으로 변경. (v0.6은 양쪽이 항상 동시에 열려 있었습니다)

### 변경 — 피버 타임

- **출구가 매 판 한쪽에만 열립니다.** 반대쪽 끝은 회색 빗금 **벽**으로 표시됩니다.
  초록 화살표 + `EXIT` 글자 / 회색 빗금 벽으로 모양이 달라 색 없이도 구분됩니다(WCAG 1.4.1).
- 방향 선택이 없는 건 그대로입니다. 타일을 누르면 **그 판의 EXIT 쪽으로 자동 발사**되고,
  에미터 화살표도 그쪽만 가리킵니다. 빔은 1개입니다.
- **출구 방향에 따라 정답이 통째로 뒤집힙니다.** 같은 레인 `· · ╱ · ·` 기준:

  | 선택 | 출구 오른쪽 | 출구 왼쪽 |
  |---|---|---|
  | 0열 | 막힘 | 1칸 |
  | 1열 | 막힘 | **2칸** |
  | 3열 | **2칸** | 막힘 |
  | 4열 | 1칸 | 막힘 |

  거울만 보는 게 아니라 **출구가 어디 열렸는지를 먼저 읽어야** 합니다.
- **최대 칸수가 3칸 → 5칸으로 늘었습니다.** 양쪽 출구일 때는 한쪽 경로가 반토막 났는데,
  한쪽만 열리면서 레인을 끝까지 관통할 수 있게 됐습니다.
  10,000판 실측 분포: 1칸 32.3% · 2칸 24.1% · 3칸 18.6% · 4칸 10.5% · 5칸 14.6%.
- **양쪽 관통 보너스 → `최적!` 보너스로 교체.** 그 레인에서 낼 수 있는 최대 칸수를 뽑으면
  점수 ×1.5와 `최적!` 연출이 붙습니다. 운이 아니라 판단에 보상이 갑니다.
  실측 거울 2열·출구 오른쪽에서 **3열(최적, 2칸) 191점 vs 4열(1칸) 102점 — 1.9배**.
- 거울은 **출구 반대쪽 끝 칸을 항상 비워 두도록** 배치합니다.
  (출구 오른쪽이면 0~3열, 왼쪽이면 1~4열) 어떤 레인이든 반드시 풀립니다.
- 실패해도 라이프·콤보 손실은 없습니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 10,000판: 출구 분포 L 5,058 / R 4,942, 출구쪽 끝칸에 거울 **0건**,
  풀 수 없는 레인 **0건**.
- 판정 정합성 5,000레인 전수 검사: "통과 = 자기와 출구 사이에 거울 없음"과
  경로 칸수(`출구 오른쪽이면 5-c, 왼쪽이면 c+1`) 모두 불일치 **0건**.
- 프레임 수동 구동으로 확인 — EXIT 마커 1개·벽 마커 1개, 빔 1개, 에미터 단방향 화살표,
  최적 위치에서 `최적!` 표시, 출구 반대편 칸은 0점·콤보 유지·라이프 유지,
  거울 없는 레인 5칸 472점, 키보드 `→`+`Space` 동작, 거울 칸 탭 거부.
- 320px 기준 피버 타일 터치 영역 **32.1 × 65.4px** (WCAG 2.5.8 통과).
- EXIT·벽 마커가 viewBox 안에 들어옴 (x 12~427 / 폭 452).
- 회귀: 일반 맵 셀 25개·사분면 84개 그대로, 피버 종료 후 5×5 복귀 확인.

---

## v0.6 — 2026-07-29

피버 레인에 양쪽 출구 도입. 방향 선택을 없애고 **타일 한 번만 누르면** 되도록 단순화.

### 변경 — 피버 타임

- **양쪽 끝이 모두 EXIT**가 됐습니다. 레인이 `[왼쪽 탈출 구역 | 막힌 구간 | 오른쪽 탈출 구역]`으로
  갈려서, 어느 쪽이 뚫려 있고 어느 쪽이 더 긴지를 읽는 판단이 생깁니다.
- **방향을 고를 필요가 없습니다.** 타일을 누르면 그 자리에서 **좌우로 동시에 발사**됩니다.
  탭 한 번에 설치와 발사가 같이 이뤄지고(v0.5는 2탭), 뚫린 쪽 빔이 EXIT로 나갑니다.
  막힌 쪽 빔은 거울에 부딪혀 위·아래로 꺾여 나가는 게 그대로 보입니다.
- **양쪽 관통 보너스** — 거울이 없는 잭팟 레인(약 17%)에서는 양쪽이 다 뚫려 점수 **×1.5**와
  `양쪽 관통!` 연출이 붙습니다. 실측 424점 vs 일반 127점.
- 거울은 이제 **1~3열(안쪽)에만** 놓입니다. 0열은 항상 왼쪽으로, 4열은 항상 오른쪽으로
  나갈 수 있어 어떤 레인이든 반드시 풀립니다. 대신 안전한 끝칸은 1칸짜리 최소 점수입니다.
- 칸당 배점을 `cells×14` → `cells×22`로 올렸습니다. 양쪽 출구가 되며 경로가 짧아진 만큼
  선택 차이를 살리기 위해서입니다. 거울이 2열일 때 **0열(1칸) 121점 vs 1열(2칸) 193점 — 1.6배**.
- 발사 버튼과 `Space`는 커서 위치에서 쏩니다(키보드·버튼 경로 유지). 설치 전에도 활성입니다.
- 실패해도 라이프·콤보 손실은 없습니다.
- 레인 기하를 좌우 대칭으로 재배치했습니다(`FPAD` 18→46, 타일 폭 70.4→64.8).
  EXIT 화살표가 viewBox 안에 들어오는 것을 확인했습니다(왼쪽 16~31, 오른쪽 421~436 / 폭 452).

### 수정

- `flyBeam`이 발사 토큰을 **증가**시키던 것을 **캡처**로 바꿨습니다. 이전에는 두 번째 빔이
  첫 번째 빔의 토큰을 무효화해 좌우 동시 발사가 불가능했습니다.
  이제 무효화는 `stopBeam()`만 담당하고, `fire()` 시작 시 이를 호출합니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 8,000회: 0열·4열에 거울이 놓인 경우 **0건**, 풀 수 없는 레인 **0건**,
  거울 0개 1,343 / 1개 3,361 / 2개 3,296(잭팟 16.8%).
- 판정 정합성 4,000레인 전수 검사: "왼쪽 탈출 = 자기보다 왼쪽에 거울 없음"(우측도 대칭)과
  시뮬레이션 결과 불일치 **0건**.
- 프레임 수동 구동으로 확인 — 탭 즉시 빔 2개 생성, 에미터 양방향 화살표,
  잭팟 시 `양쪽 관통!` 표시, 막힌 칸에서 점수 0·콤보 유지·라이프 유지,
  거울 칸 탭 거부와 안내 문구, 키보드 `←`+`Space` 동작.
- 320px 기준 피버 타일 터치 영역 **32.1 × 65.4px** (WCAG 2.5.8 통과).
- 회귀: 일반 맵 셀 25개·사분면 84개 그대로, 피버 종료 후 5×5 복귀 확인.

---

## v0.5 — 2026-07-29

피버 타임을 1×5 레인 미니게임으로 교체. **일반 5×5 맵은 변경하지 않았습니다.**

### 변경 — 피버 타임

기존 피버는 "아무 타일이나 마구 두드리기"라 판단할 것이 없고 5×5 사분면 조작과도 어긋났습니다.
이제 피버가 시작되면 보드가 **1×5 레인**으로 바뀌고, 출구는 **항상 오른쪽 끝**입니다.

- **타일 선택 → 발사** 2탭 루프. 성공하면 즉시 새 레인이 깔려 6초 동안 반복합니다.
- **거울은 반사판이 아니라 차단벽입니다.** 한 줄이라 거울에 닿은 빔은 위·아래로 꺾이는데
  그 방향이 곧 벽 밖이라 그대로 이탈합니다. 이 성질을 그대로 규칙으로 썼습니다.
  → *가장 오른쪽 거울보다 오른쪽 타일에 설치해야 통과합니다.*
- **왼쪽에서 쏠수록 고득점** — 경로가 길어집니다. 콤보 5 기준 0열 258점 vs 4열 148점(약 1.7배).
  최적해는 "가장 오른쪽 거울 바로 다음 칸"이라, 한눈에 읽고 즉시 누르는 판단 게임이 됩니다.
- 실패해도 **라이프·콤보 손실이 없습니다.** 점수만 못 얻고 다음 레인으로 넘어갑니다.
- 레인은 거울 1~2개를 0~3열에만 배치해 **4열이 항상 비어 있어 반드시 풀립니다.**
  5,000회 생성 검사에서 4열에 거울이 놓인 경우 0건, 풀 수 있는 레인 100%.
- **회전 버튼이 필요 없어졌습니다.** 피버 중에는 발사 방향이 오른쪽으로 고정되므로
  회전 버튼이 자동으로 비활성화되고, 발사 버튼만 살아 있습니다.
- 키보드: `←` `→` 타일 이동 · `Enter` 설치 · `Space` 발사.

**터치 타깃이 크게 좋아졌습니다.** 320px 폭 기준 피버 타일은 **34.9 × 65.4px**로,
같은 화면에서 5×5 사분면 높이 17.8px(WCAG 2.5.8 미달)과 대비됩니다.

### 검증

- 콘솔 에러 0건.
- 레인 생성 5,000회: 4열 거울 0건, 거울 1개 2,495 / 2개 2,505, 풀 수 있는 레인 100%.
- 판정 정합성 3,000레인 전수 검사: "가장 오른쪽 거울보다 오른쪽이면 통과"와 시뮬레이션 결과
  불일치 **0건**.
- 프레임 수동 구동으로 전 과정 확인 — 설치 하이라이트, 발사 중 재입력 잠금,
  성공 시 점수·콤보·히트 증가와 새 레인 생성, 실패 시 점수 0·라이프 유지,
  거울 칸 설치 거부, 미설치 발사 시 안내 문구.
- 키보드 `← → Enter Space` 전 과정 동작.
- 회귀: 일반 맵은 셀 25개·사분면 84개 그대로, 레벨 생성의 최소 반사 보장 50/50,
  피버 종료 후 5×5 복귀와 회전 버튼 재활성화 확인.

### 정정

v0.4에서 "1×5는 거울 반사가 성립하지 않아 채택 불가"로 적었는데, 요청의 의도는
**일반 맵이 아니라 피버 타임**이었습니다. 피버에서는 반사가 없다는 성질이 오히려
"거울 = 차단벽"이라는 명확한 규칙이 되어 그대로 채택했습니다.

또 v0.4에 적은 "사분면 탭이 WCAG 2.5.8 미달"은 재검토 결과 **위반이 아닙니다.**
2.5.8에는 같은 화면에 기준을 충족하는 대체 컨트롤이 있으면 예외라는 조항이 있고,
일반 맵에는 48px 회전·발사 버튼과 키보드 조작이 이미 있습니다. 다만 조작감 자체는
개선 여지가 남아 있습니다.

---

## v0.4 — 2026-07-29

레이저 발사 연출 강화.

### 추가

- **레이저 비행 연출** — 기존에는 빔 선이 CSS transition으로 그려지기만 했습니다.
  이제 흰색 **선단(head)**이 경로를 실제로 따라 날아가고, 빔은 그 뒤로 그려집니다.
  `requestAnimationFrame`으로 매 프레임 `getPointAtLength()`를 써서 선단 좌표를 잡기 때문에
  꺾이는 지점에서도 궤적이 정확히 따라갑니다.
  - **머즐 플래시** — 발사 순간 발사구에서 흰 섬광이 터집니다.
  - **반사 충격파** — 거울을 지나는 바로 그 타이밍에 흰 링이 퍼집니다.
    (경로상 거울 수와 링 발생 수가 일치하는 것을 확인)
  - **출구 폭발** — 성공 시 초록 링 3개가 시차를 두고 퍼집니다.
  - **소멸 연출** — 빗나가면 선단이 분홍 파티클 7개로 흩어집니다.
  - 진행 속도를 칸당 68ms → 74ms로 약간 늦춰 궤적이 눈에 들어오게 했습니다.
- 발사 토큰(`S.beamToken`)을 도입해, 연출이 도는 중에 레벨 전환·피버·게임오버가 끼어들면
  즉시 중단하고 결과 처리(`resolve`)가 두 번 불리지 않게 했습니다.
  `nextLevel()` / `startFever()` / `gameOver()`에서 `stopBeam()`을 호출합니다.
- `prefers-reduced-motion: reduce`에서는 연출 전체를 건너뛰고 결과만 즉시 표시합니다.

### 검증

- 콘솔 에러 0건.
- 프레임을 수동 구동해 계측: 선단이 `(306,386) → (271.8,306) → (226,191.8) → (226,31.9)`로
  반사 지점에서 방향을 꺾으며 이동, 반사링 2개 = 경로상 거울 2개 일치,
  출구 폭발링 3개, 완료 후 선단 제거 및 인라인 dash 해제 확인.

### 검토 결과 (구현하지 않음)

- **1×5 한 줄 배치는 거울 반사가 성립하지 않습니다.** 행 수별로 4,000판씩 시뮬레이션한 결과,
  오른쪽 끝으로 나가는 해 중 거울을 거친 비율이 1행에서 **0.0%**(최대 반사 0회, 최대 경로 3칸)였습니다.
  한 줄에서는 거울에 닿는 순간 빔이 위아래로 꺾이는데 그 방향이 곧 벽 밖이라 그대로 이탈합니다.
  2행 36.6%(최대 반사 5회), 3행 48.1%(9회), 5행 59.0%(12회)로, 반사가 살아나려면 최소 2행이 필요합니다.
- **사분면 탭이 좁은 화면에서 WCAG 2.5.8에 미달합니다.** 320px 폭에서 셀이 35.7px로 줄어
  사분면 삼각형 높이가 **17.8px**(기준 24px)가 됩니다. 방향 선택 UI 개편이 필요합니다.

---

## v0.3 — 2026-07-29

버튼 기반 조작 체계 도입, WCAG 2.2 준수, 피버 타임, 레벨 생성 규칙 강화, 효과음 전면 확장.

### 추가

- **설치 → 회전 → 발사 조작 체계** — 탭 한 번에 바로 나가던 방식을 3단계로 분리했습니다.
  - 빈 타일을 탭하면 그 자리에 **레이저를 설치**합니다. 손을 떼도 발사되지 않습니다.
    (누른 채 밀면 v0.2처럼 방향까지 한 번에 지정할 수 있습니다.)
  - `↺ / ↻` **회전 버튼**으로 90°씩 방향을 바꿉니다.
  - `▶ 발사` 버튼으로 쏩니다.
  - 설치된 위치에는 발사구 링과 방향 화살표가 표시됩니다.
- **전체 키보드 조작** (WCAG 2.1.1) — PC에서 버튼을 누르지 않고 플레이할 수 있습니다.
  `방향키` 커서 이동 · `Enter` 설치 · `Q`/`E` 좌우 회전(`R`도 지원) · `Space` 발사 ·
  `P` 일시정지 · `M` 음소거 · `H` 도움말 · `Esc` 닫기. 각 버튼에 단축키를 함께 표기했습니다.
- **FEVER 타임** — 콤보가 5의 배수가 될 때마다 6초간 발동합니다.
  아무 타일이나 연타(또는 `Space` 연타)하면 타격마다 점수와 콤보가 쌓이고,
  타격 지점에 획득 점수가 튀어 오릅니다. 획득량은 `(30 + 타격수×6) × 콤보배수`로 계속 커집니다.
- **일시정지** (WCAG 2.2.2) — `P` 또는 버튼. 남은 시간이 그대로 보존됩니다.
- **도움말 오버레이** — `H`로 열고 `Esc`로 닫습니다(WCAG 1.4.13). 여는 순간 자동으로 일시정지합니다.
- **소리 켜기/끄기** — `M` 또는 버튼. 설정이 `localStorage`에 유지됩니다.

### WCAG 2.2 적용

- **1.4.3 색상 대비 (AA)** — 배경 대비를 계산해 팔레트를 재조정했습니다.
  보조 텍스트 `--dim`을 `#6b7299`(3.7:1, 미달) → `#8b93bd`(**5.7:1**)로,
  `--pink` `#ff3d8b`(5.1:1) → `#ff6ba6`(**6.5:1**), `--green`은 **13.4:1**,
  본문 **14.6:1**, 오버레이 본문 **12.5:1**. 판독 불가였던 푸터 색(`#454b70`)도 교체했습니다.
- **1.4.11 비텍스트 대비 (AA)** — 셀·패널 테두리 `--line`을 `#1f2340`(1.3:1, 미달) →
  `#565f96`(**3.0:1**)로 올려 타일 경계가 식별되게 했습니다.
- **1.4.1 색에 의존하지 않기 (A)** — 실패 빔은 분홍색뿐 아니라 **점선**으로,
  시간 경고 바는 색뿐 아니라 **사선 패턴 + 남은 초 텍스트**로, 출구는 초록 화살표에
  **EXIT 글자**를 덧붙여 색 없이도 구분됩니다.
- **1.4.4 텍스트 크기 조정 (AA)** — 모든 치수를 `px` → `rem`으로 전환. 200% 확대에서
  가로 스크롤 없이 세로로 리플로우되는 것을 확인했습니다.
- **1.4.10 리플로우 (AA)** — 320px 폭에서 가로 스크롤 0.
- **1.4.13 호버·포커스 콘텐츠 (AA)** — 도움말 오버레이가 `Esc`로 닫힙니다.
- **2.1.1 키보드 (A)** / **2.5.7 드래그 대안 (AA)** — v0.2의 드래그 조준이 유일한 입력이었던 것을
  버튼·키보드로 완전히 대체 가능하게 했습니다.
- **2.4.7 포커스 표시 (AA)** — 모든 조작 요소에 흰색 3px `:focus-visible` 아웃라인.
- **2.5.8 터치 타깃 (AA)** — 모든 버튼 최소 44px 높이(측정값 48px, 기준 24px 초과).
- **2.2.2 일시정지 (A)** — 제한시간·애니메이션을 멈출 수 있습니다.
- **3.3.1 오류 식별 (A)** — 실패 사유를 `거울 사이를 무한히 맴돌았습니다` /
  `초록 EXIT가 아닌 곳으로 나갔습니다`처럼 구체적으로 표시합니다.
  레이저 없이 발사를 누르면 `먼저 타일을 눌러 레이저를 설치하세요`를 안내합니다.
- **4.1.2 이름·역할·값 (A)** — 조작부를 전부 `<button>`으로 구성하고 `aria-label`,
  `aria-pressed`, `aria-disabled`를 붙였습니다. 보드는 `role="application"`,
  타임바는 `role="progressbar"`.
- **4.1.3 상태 메시지 (AA)** — `aria-live` 영역에 레벨 시작, 설치 위치와 방향, 회전 방향,
  클리어·실패 사유, 피버, 게임 오버를 음성으로 안내합니다.
- `prefers-reduced-motion`에서 흔들림·플래시·스파크·빔 애니메이션을 제거합니다.

### 변경

- **레벨 생성기: 직선 정답 차단** — 거울을 하나도 거치지 않고 그냥 직선으로 쏘면 되는 판이
  나오던 문제를 막았습니다. 두 조건을 추가했습니다.
  1. 출구가 걸린 행/열에 거울이 최소 1개 (요청하신 규칙 그대로).
  2. 정답 경로의 반사 횟수가 최소 `1 + ⌊레벨/4⌋`회(최대 4회).

  레벨 1~30에서 각 40판씩 검사해 **직선 정답 0건**, 출구 행/열에 거울이 없는 판 **0건**,
  실측 최소 반사 횟수는 레벨 1에서 1회, 레벨 20 이상에서 8회였습니다.
  생성 비용은 판당 0.08~0.83ms로 이전과 같은 수준입니다.
- 안전망(생성 실패 시 폴백)도 거울 1개를 포함한 배치로 교체했습니다. 실측 도달 0회.
- 실패 시 설치된 레이저를 해제해 다음 판이 깨끗하게 시작됩니다.

### 효과음

18종으로 확장했습니다. 마스터 게인과 밴드패스 노이즈 제너레이터를 추가해,
기존 사각파 단음 위주에서 벗어나 톤 + 노이즈 조합으로 만듭니다.

설치 · 회전 · 발사(하강 스윕+노이즈) · 경로 이동음 · **거울에 닿을 때마다 반사음(음정 상승)** ·
출구 통과 · 클리어 · 퍼펙트 팡파레 · 미스 · 시간 초과 · 카운트다운 틱(마지막 3초) ·
레벨업 · 피버 진입 · 피버 타격(음정 상승) · 피버 종료 · 게임 오버 · 일시정지 · 재개 · 버튼 · 커서 이동.

### 수정

- **피버 종료 후 900ms 동안 점수가 계속 들어가던 버그** — `endFever()`가 `S.phase`를 바꾸지 않아,
  게이지가 0이 된 뒤 다음 레벨이 시작될 때까지 연타가 그대로 득점됐습니다.
  실측으로 확인 후 수정했고, 지금은 종료 직후 10회 연타에도 획득 점수 0입니다.
- **실패 빔의 점선 패턴이 보이지 않던 문제** — 진행 애니메이션용 인라인 `stroke-dasharray`가
  CSS 점선을 덮고 있었습니다. 애니메이션이 끝나면 인라인 값을 걷어내도록 수정했습니다.
- 키 핸들러의 `e.target.closest` 호출에 방어 코드를 추가했습니다(target이 Element가 아닐 때).

### 검증

- 콘솔 에러 0건.
- 키보드만으로 `방향키 이동 → Enter 설치 → E 회전 → Space 발사 → CLEAR` 전 과정 재현.
- 탭 설치 후 발사되지 않음, 버튼 회전, 버튼 발사, 미설치 발사 안내 확인.
- 콤보 5에서 피버 진입 → `Space` 8연타로 1,144점·8히트·콤보 13 확인 → 종료 후 무득점 확인.
- 일시정지 중 남은 시간 보존, 도움말 `Esc` 닫힘, 소리 토글 확인.
- 320×568 / 390×844 / 텍스트 200% 확대 모두 가로 스크롤 0.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초, 최소 반사 4회가
  모두 상한에 닿습니다.
- **정답 탐색은 여전히 단일 전략입니다.** 거울 반사가 가역적이라 모든 정답이 퍼펙트 경로의
  뒷부분이고, 퍼펙트 정답은 항상 1개뿐입니다. 코인 타일이나 출구 2개 같은 기믹이 있어야
  `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).

---

## v0.2 — 2026-07-29

모바일 조작성 개선과 코드 리뷰에서 나온 버그 수정.

### 추가

- **드래그 조준 입력** — 빈 칸을 누른 채 원하는 방향으로 밀면 그 방향이 조준되고, 손을 떼는 순간 발사됩니다.
  누르는 동안 조준된 사분면이 청록색으로 채워지고 화살표가 밝아져, 쏘기 전에 방향을 확인·수정할 수 있습니다.
  보드 바깥에서 손을 떼면 발사가 취소됩니다.
- **터치 환경 방향 가이드** — `@media (hover:none)`에서 빈 칸마다 4방향 화살표를 옅게(opacity .12) 상시 표시합니다.
- **최고 점수 저장** — `localStorage`(`mirrorrush.best`)에 기록합니다. 시작 화면과 GAME OVER 화면에 `BEST`를 표시하고,
  경신 시 `★ 신기록!`을 띄웁니다. localStorage 접근이 차단된 환경에서도 조용히 동작합니다.
- **좁은 화면 대응** — 세로 620px 이하(가로모드 등)에서 HUD를 압축하고 푸터를 숨깁니다.
- 노치 대응(`env(safe-area-inset-*)`), `prefers-reduced-motion` 존중, 버튼 최소 터치 영역 44px.

### 변경

- 보드 크기를 뷰포트 높이 기준으로도 제한(`min(470px, max(230px, 100dvh - 250px))`).
  320×568부터 가로모드까지 스크롤 없이 한 화면에 들어옵니다.
- HUD 라벨 `최대 N칸` → `목표 N칸`. 자기 기록으로 오해하기 쉬웠습니다.
- 클리어 플래시에 실제 적용된 콤보 배수를 표시(`6칸 · x1.15 · +221`).
  HUD의 COMBO는 *다음* 판 배수라 획득 점수와 계산이 맞지 않아 보였습니다.
- 조준 시 짧은 클릭음 추가.

### 수정

- **`requestAnimationFrame` 루프 누수** — `nextLevel()`이 `stopTimer()` 없이 `tick()`을 호출해,
  `startGame()`이 재진입하면 이전 타이머 루프가 고아가 된 채 영구히 돌았습니다.
  실측 시 `stopTimer()` 호출 후에도 600ms 동안 6프레임이 계속 갱신됐고, 수정 후 0프레임입니다.
  `nextLevel()`에 `stopTimer()`를 추가하고 `startGame()`에 재진입 가드를 넣었습니다.
- **확대 차단 해제 (접근성)** — 뷰포트의 `maximum-scale=1, user-scalable=no`를 제거했습니다(WCAG 1.4.4).
  더블탭 줌은 `touch-action`이 이미 막고 있습니다.
- 발사·시간초과·게임오버 시 조준 상태를 정리하도록 수정.
- 데드코드였던 `.locked` CSS 규칙과 클래스 토글 제거. `fire()`가 이미 `gQuads`를 비웁니다.
- `.cellbg`의 중복 `rx` 선언 정리.

### 검증

- 콘솔 에러 0건.
- 레벨 생성 성능: 레벨 1~30에서 판당 0.1~0.7ms. 800회 시도 안전망(빈 그리드 폴백) 도달 0회.
- 드래그 조준을 PointerEvent로 재현해 `누름 → 방향 전환 → 발사 → CLEAR`까지 확인.
- 뷰포트 320×568 / 390×844 / 844×390(가로)에서 레이아웃 확인.

### 알려진 사항 (다음 버전 후보)

- **레벨 20 이후 난이도가 완전히 고정됩니다.** 거울 13개, 목표 14칸, 제한시간 6초가 모두 상한에 닿아
  이후로는 무한 반복 구간입니다.
- **정답 탐색이 단일 전략입니다.** 거울 반사가 가역적이라, 정답 530개를 검증한 결과
  **모든 정답이 퍼펙트 경로의 뒷부분(suffix)이고 퍼펙트 정답은 항상 정확히 1개**였습니다.
  즉 "출구에서 역추적" 외에 판단할 것이 없어, 퍼펙트 2배 보너스가 사실상 이진 판정으로 작동합니다.
  코인 타일·출구 2개·반사 횟수 제한 같은 기믹을 넣으면 `최장 경로 ≠ 최고 점수`가 되어 점수 판단이 생깁니다.
- 라이프 회복 수단이 없습니다(3개 고정).
- 키보드 플레이 미지원(SPACE 재시작만).

---

## v0.1 — 최초 버전

- 5×5 거울 방 레이저 퍼즐. 빈 칸의 사분면을 클릭해 해당 방향으로 발사, 초록 출구로 탈출하면 클리어.
- 전수 시뮬레이션 기반 레벨 생성기 — 풀 수 있는 판만 출제하고 해의 개수와 최장 경로 길이를 제어.
- 레벨·점수·콤보·라이프 HUD, 제한시간 바, SVG 빔 애니메이션, WebAudio 효과음.
- 의존성 없는 단일 HTML 파일.
